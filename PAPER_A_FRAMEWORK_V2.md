# 论文 A 框架 v2：精度–控制保真度张力与物理嵌入折中

> 状态：规划稿 v2（2026-08-18，grill 四决策后重设计）。取代 ADHOC_PAPER_PLAN.md 中论文 A 的原主题（F1/F2/F3 修复链叙事）。
> 决策来源：①两文分家+架构稿分层语言吸收 ②A 先 B 后、B 引用 A ③phase1 概率预测框架重协议后作精度端点进 A ④主题升级为"张力+折中曲线"。

## 当前状态与共识（2026-08-18 傍晚，供 Codex 同步）

**已定共识**：
1. **两文分家**：论文 A（本仓，先发表）= 张力+折中曲线；论文 B（主仓，后行）= 系统层框架（架构稿为骨架），引用 A 的动力学与残差边界结论
2. **A 主题** = 闭环数据上预测精度与控制保真度的可测张力：机制（Q32 链）+ 测量协议（F3）+ 物理嵌入折中解（FIXB+qnav/h_now）
3. **Direct WM 定位修订（用户裁定）**：不单作精度端点竞争对手——它是**边界/初始化/σ 供应方**（架构稿 O_φ+B_ψ 角色）+ 曲线纯数据点（响应弱点在 ±2% 对象阶跃下如实量化）。能力互补而非精度竞争
4. **统一协议**：残差/神经组件内部 RevIN、物理灰盒真实尺度；对比口径 = 物理空间、统一折、统一 horizon 的精度与响应联合表；reserved [40000,50000) 禁触
5. **目标期刊** Applied Energy；干态证据补强待有新数据再议

**进行中**：
- Direct WM v2 冻结合同（Codex `1d8251e`）执行中；执行方一行修复（`ef1d366`，eval_sensitivity 补 @torch.no_grad()，executor-side fix 待 Supervisor 复核）
- Q32-T 科学裁决仍待 Codex

**对 Codex 的请求**：
1. 复核 `ef1d366`（executor-side fix：1 行装饰器，折/种子/协议零改动，pytest 5/5 + smoke 通过）
2. Q32-T 三面板的 supervisor 裁决（raw artifacts 已在 `af2b168`/`92d751e` 回传）
3. Direct WM v2 结果落地后，下一阶段目标 = **精度与响应联合表**（统一折/统一 horizon 下 double_w/qnav/h_now/conservative_now/evap_only/Direct WM 六点重测）——曲线成形前不再新增探针

## 0. 一句话定位

本文检验一个工作假设：闭环数据上的预测精度与动作通道物理保真度可能存在可测张力。Q32 链给出机制候选与测量协议；统一协议下的端点和折中曲线仍待完成，不能提前写成已证实结论。

**英文定位建议**：The Precision–Control-Fidelity Trade-off in World Models Learned from Closed-Loop Industrial Data: Mechanism, Measurement, and a Physics-Embedded Resolution

**边界表述**（沿用架构稿）：disturbance-conditioned, support-aware, control-usable；不声称任意策略下完整 `do(valve)`、完全 plant identification 或未测喷水真值恢复。

## 1. 曲线定义（论文的视觉主轴，审稿人看这张图）

- **X 轴 = 残差权威/输入权限**（从"读 W + 非守恒自由注热"→"动作隔离 + 固定注入位 S"→"无残差"）
- **Y1 = 预测精度**（统一协议 rollout/MAE）
- **Y2 = 控制保真度**（动态响应域：增益符号、τ63、闭环收敛、换向）
- **采样点**（按 X 排序）：
  - `double_w`（精度上界，读 W，3.39/1.93 旧协议）
  - `qnav`（动作隔离残差上蒸发底座，rollout 2.463 旧协议）
  - `h_now`（诚实配置：h-only + 不读 W，4.22/2.90 旧协议）
  - `conservative_now`（守恒型残差，5.01≈evap_only——非守恒注热被量化）
  - `evap_only`（纯物理端点：B1 两相偏差 3.07°C、B4 干态正确退化、湿开环 τ63=1030s、rollout 12.7）
  - `Direct WM`（纯数据端点候选：40 列单目标、β-NLL 概率输出；v1 协议失效，待 v2 重跑）
- **候选折中区域 = qnav/h_now**；最终落点须由统一折、统一 horizon 的精度与响应联合表选择。

> **Direct WM 定位修订（2026-08-18 用户裁定）**：不单作"精度端点竞争对手"。能力分解结论——物理灰盒缺的恰好是 Direct WM 的长项，反之亦然：
> ① 响应结构（增益/τ63/慢动态）：物理有、Direct WM 弱（直接多步头末端收敛条件均值，t1 近零响应）
> ② 绝对水平精度：Direct WM 强（step0 ~0.2°C）、物理差
> ③ 初始状态恢复（observer）：Direct WM 40列上下文天然能干、物理无
> ④ 未来边界+σ：Direct WM 有、物理无
> ⑤ 可自回滚：物理有、Direct WM 无（teacher-forced）
> **定位 = 边界/初始化/σ 供应方（架构稿 O_φ+B_ψ 角色）+ 曲线纯数据点（响应弱点在对象阶跃协议下如实量化）。** 折中方案从"两模型抢精度"变为"职责互补"。未来遮蔽仅对"未知动作纯预报"场景有意义，修不了末端保守（保守来自不可约扰动+损失平均）。

## 2. 四条贡献卡片

1. **F1 动作污染机制**：闭环数据下残差学成隐式控制器（三步对照 e0 −0.047 → qh +0.073 → qnaw −0.191）
2. **F2 张力（中心论点）**：精度与控制保真度沿残差权威滑动的折中曲线；Q32 链给出机制——读 W 捷径、非守恒注热、反馈翻转（干态 8/8→2/8，replay 全恢复）、replay 工程解失败、增益压缩 30-75%
3. **F3 测量协议**：动态响应域验证（差分阶跃 + 调度式代理 PI 闭环 + 锚点门槛 + 预注册纪律），用于分离对象、控制器实现与初始化误差
4. **物理嵌入折中解**：蒸发干燥灰盒（FIXB：液滴状态+壁面供热+干燥度门控）+ 动作隔离残差（qnaw/qnav no_v12）——湿态 τ63=480s 进窗、rollout 2.463、两相偏差 3.07°C

## 3. 证据映射（Q32 归因链 → 论文叙事）

| 探针 | 结论 | 论文中位置 |
|---|---|---|
| Q32 筛查 | 读W与非守恒注热提供条件预测优势；h_now 是权限更干净候选；干态折分裂 | §5 张力机制 |
| Q32-R 反馈归因 | 干态符号翻转=残差对动作后代状态的反馈反应；replay 8/8 恢复 | §5 机制（翻转从"未解病灶"→"已归因"） |
| Q32-S 共享扰动环 | shared≈physical；该 replay 实现未改善干态，不外推否定全部扰动分离结构 | §5 反面结果 |
| Q32-T 三面板 | physical/shared 对象方向在该网格一致但 live/F1 干态不稳；deadband/LPF 抑制换向且 anti-windup 修复特定湿态偏差；初始化误差只支持继续验证状态估计 | §5 + §9 讨论 |
| F3 协议 | 金标准锚点 + 门槛纪律 + 边缘 FAIL 如实记录 | §7 方法学 |

## 4. 结构（9 节，吸收架构稿分层语言）

1. Introduction：闭环数据世界模型的控制可用性缺口；点wise 精度 ≠ 控制保真
2. Problem formulation：estimand 分层（SP/控制器输出/阀位/有效喷水）、输入权限、支持域声明；分层术语与架构稿对齐（observer/boundary/controller/dynamics/residual 职责分离）
3. 对象与数据：伊敏超临界直流炉、41 列 10s 闭环窗口、湿态/超临界并存、POU107 PI 调度
4. 物理嵌入动力学：焓+质量守恒 24 参数 → 蒸发干燥（FIXB）；B1-B5 预注册
5. 残差与张力机制：F1 污染 → F2 六实验全映射 → Q32 链归因
6. 折中曲线与落点：统一协议下端点与中间点重测；曲线图；折中解
7. 动态响应域验证协议（F3）：方法学贡献
8. 结果：精度表 + 响应表 + 闭环 case + 曲线图（图表全英文）
9. 讨论：初始化误差与 observer 需求（指向论文 B 系统层）、干态边界（n=4 事件验证受限）、单机组单窗口局限

## 5. 硬工作清单（依赖排序）

1. **统一评测协议（已定 2026-08-18）**：残差/神经组件内部用 RevIN（phase1 原生），物理灰盒用真实尺度；对比口径 = **物理空间 rollout/MAE**，同一数据切分与窗口。端点重测即在此协议下进行
2. **端点重测**：Direct WM **移植一版进 adhoc 仓**（主仓 src/world_model.py v2 + exp_023 配置为移植蓝本）后在新 split 重训（修复 test 反复使用问题）+ evap_only/qnav/h_now/double_w 同协议 rollout
3. **头对头控制可用性**：各点在动态响应域协议下的增益/τ63/闭环对比（部分已有，按统一协议重跑）
4. **曲线图 + case 图**
5. 写作（目标期刊 **Applied Energy**，图表全英文）

## 6. 风险（审稿人视角，提前备好）

1. **折中点可能在两端点均不占优**——价值需由统一协议下的控制可用性证明。当前仅部分湿态子集较平滑，干态仍有阀饱和与折分裂，不能写“全场最平滑”。
2. **phase1 动作敏感性 −0.382 方向正确**——审稿人可能说"纯数据模型动作方向也对"。正面对照证据在主仓 MS3（A 侧 response non-collapse 3/3 FAIL、B 侧 PASS），跨仓引用须 [PHASE-REF] 声明协议差异
3. 0.528（phase1）与 2.463（adhoc）协议不可比——所有跨仓数字入文前必须重测，正文禁止混用
4. 单机组单窗口，无跨机组泛化证据
5. Q32 的诚实配置比捷径配置多付 ~25% RMSE——曲线必须如实画价格，不藏

## 7. 与论文 B 的接口

- 论文 A 完全独立发表，不依赖 B
- 论文 B（系统层框架，架构稿为骨架）后行，引用 A 的动力学与残差边界结论；B 的 L1 层沿用 phase1 框架（A 重协议后的数字可被 B 引用）
- 共享术语：L1-L5 证据阶梯、observer/boundary/controller/dynamics/residual 分层、D0-D6 候选矩阵口径
- A 的 §9 讨论给 B 留两个待验证接口：初始化误差→状态估计/observer 假设；残差注入位 S 门控→系统层残差隔离

## 8. 开放决策（2026-08-18 已拍板）

- [x] 统一协议归一化：**残差用 RevIN、物理用真实尺度**；对比口径=物理空间 rollout/MAE 同切分
- [x] 精度端点 Direct WM 代码归属：**移植一版进 adhoc 仓**（world_model.py v2 + exp_023 蓝本）
- [x] 目标期刊：**Applied Energy**
- [ ] 干态控制可用性证据补强：**目前无新增数据，推迟**（Q32-T 对象层已给，闭环层干态证据仍薄，待有数据再议）
