# NOTES — ad-hoc 2 焓探针完整证据链（Step 3 收尾，草稿待审）

> 状态：草稿 v0，2026-08-16。设计稿 DESIGN.md 预注册冻结（判决标准不可改）。
> 结论一句话：**3 段集总焓灰盒在单相干态平衡处有效（±1.3°C），两相湿态与长时递归 rollout 失效；失效根因经修复链 + 分层消融 + 干态退化诊断三关定位为"模态内热分配漂移 + 共享 pm 斜率跨模态外推错误"，属解析参数化表达力上限，非代码缺陷。判决 FAIL 按预注册门槛原样记录。**

## §0 文件清单与复跑命令

- 仓库：`~/.hermes/workspace/adhoc2_lumped_enthalpy`（纯本地 git，无 remote）
- 提交链：`4aec407`(Step1主赛) → `b74f866`(e0四修复) → `48443be`(h_liq网格) → `309d98c`(fixA五轮) → `2967bbf`(diag_trace) → `125562e`(方向②分层消融) → `17e8a01`(float64协议) → `6fd76f5`(gitignore) → 本提交(Step3收尾: 本 NOTES + 08_diag_dry)
- 环境：`~/anaconda3/envs/Alloftime/bin/python`（torch 2.11.0+cu130, iapws 1.5.5）
- 数据：`~/Desktop/AI/时序预测/TIME-all-model/TCN-Improved-GRU/data/mainT/A侧主汽温全数据_cleaned_10s.csv`，SHA256 `632bf7e53dec59c1...`，A侧主汽温 41 列 10s，窗口 5.8 天
- 复跑核心：
  ```bash
  cd ~/.hermes/workspace/adhoc2_lumped_enthalpy
  ~/anaconda3/envs/Alloftime/bin/python 01_surrogate.py          # 生成 iapws_surrogate.npz（已冻结）
  ~/anaconda3/envs/Alloftime/bin/python 02_train.py --fast 0     # e0 训练 seed0（fast=烟测）
  ~/anaconda3/envs/Alloftime/bin/python 07_strat_ablation.py     # 分层消融全量（e0_pre/e0_post/v0/v2/v2o）
  ~/anaconda3/envs/Alloftime/bin/python 08_diag_dry.py           # 干态退化诊断（方向E）
  ```
- 关键产物：`out/step1_summary.json`（主赛判决）、`out/fixA_summary.json`（修复链终态）、`out/strat_ablation_summary.json`（分层消融 + decision_checks）、`out/strat_pre_fixA.json` / `out/strat_post_fixA.json`（前后分层原始）、`out/legacy_*/`（每轮修复快照）

## §1 协议（节选自 DESIGN.md，全文见设计稿）

- 判决门槛（预注册冻结）：P1 滚动 RMSE(main) ≤ 2.48；P2 带外率 ≤ 0.5%；P3 物理序违例 ≤ 46.11%（真值地板 41.1% + 5pp）
- 模型 e0：Fan2020 骨架 3 段集总焓 + 3 金属 + 制粉一阶 + 喷水代理，h 空间 IAPWS-IF97 闭合，半隐式欧拉 5 子步
- 基线：ad-hoc 1 的 GRU 黑盒 v0/v2/v2o，同窗口/切分/seeds
- 变体 e1/e2/e3（金属/动作通道/流量校准消融）：Step2 未执行——Step1 判决 FAIL 后转向修复链，详见 §4

## §2 探查证据

### §2.1 Step1 主赛（4aec407）：整体 FAIL
- P1 11.78 > 2.48 FAIL；P2 58% > 0.5% FAIL；P3 物理序全过
- 首步 RMSE 12.8°C（"第一步就崩"）→ 后续修复链第一目标

### §2.2 修复链（b74f866 + 48443be）：修掉 4 个真 bug，暴露结构漂移
| 修复 | 内容 | 效果 |
|---|---|---|
| Tm 平衡初始化 | 初始 Tm=ts+k·rB/3600/UA+dTm（模型自身吸引子），dTm 先验 20→1 | 首步 12.8→2.3°C |
| M 不可辨识死锁 | 先验 500→5000（τ≈35s），梯度解锁，M 学到 8730±10 | M 收敛 |
| h_liq 多项式精度 | deg6 多项式 float64→float32 误差放大 10.7 kJ/kg → 1D 网格插值 maxerr 0.0003 | 喷水焓修正 |
| P3 门槛缺陷 | 预注册含方向相反物理对，真值地板 98.6% 谁也过不了 → 改物理对+地板+5pp | P3 可判 |

- 修复后：首步 2.3 ✓，但 P1 11.8 / P2 58% 仍 FAIL。窗口内 5–10 min 结构性漂移：湿态 sh1_in −16°C / main +17°C（段0欠热 + 线性 k 分配链式补偿 + τ_metal≈184s 吸引子偏置）

### §2.3 fixA 五轮结构修复（309d98c）：修好湿态，干态恶化，rollout 仍 FAIL
| 轮 | 改动 | 结果 |
|---|---|---|
| 1 | k0 单独分模态 | 被 loss 拧反（链式补偿参数层重现）。诊断法：±5% 参数扰动看窗口 loss 方向 |
| 2 | 全 3 段 k 硬切换 | 窗口指标涨，但金属 τ≈177s 追不上 k 跳变 → rollout 相位振荡 ±20–25°C。窗口与递归 rollout 分叉 |
| 3 | k+th 双分模态 + sigmoid 平滑（K_BLEND=0.3） | 干态平衡仿真 ±1.3°C（从 +15 修到 0） |
| 4 | +共享 pm 斜率 bX（tanh 有符号，PM_REF=20.5） | 需求 k 回归 R²：k0 0.32/k1 0.11/k2 0.84，b2=+0.022e6 与回归一致 |
| 5 | 喷水硬质量守恒 Dsw1+Dsw2=KAPPA·W | 软校准压不住 1.8× 喷水过量 → th 只决定分流比 |

终态（3 seeds，std 0.044）：单步 1.32 / win60 6.46 / P1 12.72 FAIL / P2 0.492 FAIL / P3 0.0 PASS。学得参数（seed0，3 seeds 一致）：k(湿)=(2.27,0.66,0.71)e6，k(干)=(2.12,0.73,0.68)e6，b=(0.011,0.016,0.026)e6/MPa，th1d=6.9/th2d=26.6。

### §2.4 方向②分层消融（125562e，验证 6/6 PASS）：干态才是最大缺口
分层定义：wet = 分离器压力 ≤ 22.064 MPa。测试段 wet 91.7%（rollout 971/1800 步、窗口 911/994，跨临界窗仅 1.2%）。

| 指标 | 层 | e0-pre | e0-post | v0 | v2 | v2o |
|---|---|---|---|---|---|---|
| rollout rmse_main | wet | 10.10 | 7.05 | 7.12 | 3.29 | 3.32 |
| rollout rmse_main | dry | 13.49 | **17.11** | 5.47 | **2.44** | 2.44 |
| rollout band | wet | 0.358 | 0.208 | 0.478 | 0.014 | 0.029 |
| rollout band | dry | 0.842 | 0.824 | 0.271 | **0.000** | 0.000 |
| win60 rmse_main | wet | 14.10 | **5.90** | — | 3.95 | 3.98 |
| win60 rmse_main | dry | 7.98 | 10.84 | — | **1.75** | 1.81 |
| first rmse_main | wet | 2.39 | 1.23 | — | 1.36 | 1.33 |
| first rmse_main | dry | 1.33 | 2.00 | — | **0.196** | 0.193 |

决策证据：①干态（纯单相，无两相物理）gap 14.67 vs 湿态 3.76——**"两相假设是唯一短板"被证伪**；②fixA 是层间权衡：湿态 win60 14.10→5.90 修好，干态 7.98→10.84 恶化（83 个干窗被 91% 湿态窗带偏）；③漂移曲线：湿态 sh1_in 仍负漂 −19°C（段0欠热残留），干态 main 单调漂到 +11.3°C。

### §2.5 方向E 干态退化诊断（08_diag_dry.py，本轮）：不是 bug，是共享 pm 斜率外推错误
用户要求先查代码。三关排查，代码无缺陷（参数 3 seeds 一致、k_of 实现与设计一致、上轮独立验证 6/6 PASS 覆盖 eval 路径），退化机制定量定位：

**A. b 消融（恒定外生 120 步吸引子偏差，30 干窗样本）：**
| 消融 | sh1i | sh1o | sh2i | sh2o | main |
|---|---|---|---|---|---|
| b×0 | +6.98 | +1.56 | −2.14 | +0.39 | **−2.73** |
| b×0.5 | +8.24 | +2.81 | +1.36 | +3.78 | +4.64 |
| b×1.0（现状） | +9.5 | +4.06 | +4.9 | +7.21 | **+12.1** |

→ 共享 pm 斜率 b 单独贡献干态 main 吸引子 ~+15°C。b=0 时干态吸引子反而偏冷 −2.7°C。

**B. 喷水校正需求 k 斜率（湿/干分模态逐步反算）：**
| 模态 | k0 需求斜率 | 模型 b0 | k1 需求斜率 | 模型 b1 | k2 需求斜率 | 模型 b2 |
|---|---|---|---|---|---|---|
| wet | +0.006 | +0.007 ✓ | +0.028 | +0.018 | +0.020 | +0.025 |
| dry | **−0.046** | **+0.011 ✗ 符号反** | +0.006 | +0.018 | +0.021 | +0.025 ✓ |

→ 干态 k0 需求随 pm 下降（负荷↑、段0焓差↓），模型 b0 却随 pm 上升。b 是全局共享参数，91% 湿态样本支配拟合，干态外推失效。干态均值偏差：k0 +10%（2.17 vs 1.97）、k1 −13%（0.79 vs 0.91）、k2 +15%（0.78 vs 0.68）。

**C. 排除项：** sigmoid 混合带非主因——rollout 干步 a>0.1 仅 5.5%、均值 0.03；干窗起点 pm 均值 24.5（远离 22.064 混合带）。

## §3 结论（每条带证据指针）

1. **e0 灰盒判决 FAIL 是结构性的，不是代码缺陷。** 证据：修复链修掉 4 个真 bug 后首步/窗口/模态均值平衡全部大幅修复（§2.2-2.3），残留失败全部归因于解析参数化的表达力：模态内热分配漂移（k1 需求跨湿相位 ±16%，协变量 R²<0.2）+ 共享 pm 斜率跨模态外推错误（§2.5B，b0 干态符号反）。
2. **"两相假设是唯一短板"被证伪。** 干态（纯单相）是 e0 与黑盒基线的最大差距层（rollout 17.11 vs 2.44，gap 14.67；湿态 gap 仅 3.76），§2.4 决策证据①。
3. **适用边界（论文结论素材）：** 模型在**单相干态平衡点**有效（平衡仿真 ±1.3°C，§2.3 轮3）；两相湿态窗口（段0 欠热 −19°C 残留）+ 长时递归 rollout（P1 12.72）失效；跨模态热分配参数无法用单一解析式统一表达（b0 干态符号反 + k1 湿相位 ±16% 漂移）。
4. **黑盒基线两种模态都好**（2.4~3.3），数据里有信号，瓶颈是灰盒结构不是数据（§2.4 总表）。
5. **E 修复评估：便宜可修、但救不回判决。** 若把 b 分模态（b_dry 独立或干态置 0）+ k0d 下修至 ~2.0e6，干态吸引子偏差预计 +12 → ~0°C，win60 dry 预计 10.84 → ~7。追平 v2（1.75）仍需补上 k1 湿相位 ±16% 的工况漂移（R²<0.2），超出解析参数化能力。P1 判决不会翻转。

## §4 局限与噪声边界

- **窗口单一**：训练 3.5 天 / 验证 1 天 / 测试 5.8 天同一机组，无跨机组泛化验证
- **变体消融未执行**：DESIGN.md 预注册的 e1（−金属蓄热）/e2（−动作通道）/e3（−流量校准）因主赛 FAIL 后转入修复链未跑——金属 τ 与动作通道的贡献未单独量化（e1 是"金属惯性是否承重"的直接证据，属未闭合环节）
- **需求 k 反算依赖模型自身 th 分流比**（§2.5B）：Dsw1/Dsw2 用 th1d/th2d 计算，若 th 辨识有偏则需求 k 有系统偏——但 b 消融（§2.5A）不依赖分流假设，主结论不受影响
- **干态样本稀缺**：83 干窗 / 994（8.4%），干态参数方差必然偏大；3 seeds 参数一致（std 0.044）说明此窗口下可复现
- **基线 v2=2.48 门槛来源**：单种子点估计 ±15% 跨进程方差（预注册已知风险 5）
- **10s 采样混叠**：M_i/D 秒级驻留与采样同级，快参数不可辨识风险（预注册已知风险 2）——M 收敛到 8730~13468 说明尚可，但 tauB=90s 级参数未单独验证

## §5 对主线启示（仅记录，未经主线评审，不视为授权）

1. **灰盒焓结构的能力边界已量化**：单相干态平衡 ✓ / 两相湿态窗口 ✗ / 递归 rollout ✗。主线若用灰盒做 MPC 世界模型，需限定工况（干态、短时窗），或按方案 D 把 k_i 参数化为工况函数（牺牲解析可解释性换表达力）。
2. **修复链方法论可复用**：时间尺度分层诊断（首步→窗口→rollout）+ 模态分层消融 + 参数扰动诊断（±5% 看 loss 方向）+ 平衡吸引子消融——四件套在 3 天内定位了一个非 bug 的结构性失效，可作为其他灰盒探针的标准流程。
3. **跨模态共享参数的教训**：任何"全局共享 + 线性外推"的参数（此处 b）在模态占比 91/9 时必被多数模态支配，少数模态外推失效——设计时应预注册"分模态 vs 共享"的消融决策点。
4. **修复后变差 ≠ bug**：fixA 使干态 7.98→10.84 恶化，一度疑似代码缺陷；方向 E 用 b 消融 30 分钟内定位为共享斜率外推。工程上"整体更好、局部更差"的结构改动需要分层指标来裁决（§2.4 层间权衡表）。

---

# §6 控制嵌入修复链与 TF 提取（2026-08-17 追记，提交 606823a→f1a3922）

## §6.1 起点：Step⑤ 动态响应域验证（11_step5_response.py + 11b 归因）

点wise RMSE 过关 ≠ 控制可用。用差分阶跃 (step−baseline) + 真实 POU107 PI 闭环 + exp_099 实测锚点验证 qh 模型 → **G1/G2/G3 全 FAIL**，三病灶：

1. **残差污染动作通道**：湿态 v2+5% 开环阶跃 main 末值 **+0.073**（符号翻转），归因实验坐实——e0 无残差 −0.047 ✓ / qh 带残差 +0.073 ✗ / qnaw 去 v 特征 −0.191 ✓。机制 = confounding by indication：闭环数据中阀门动作总与扰动并发，残差学到"v↑→注热"的非因果捷径；开环反事实注入时捷径触发。灰盒本体不受影响（阀门通道是方程结构保证的因果）
2. **喷水路径无时间结构**：τ63=10s、θ=0，闭环响应比实测快 2-4×
3. **W 硬守恒缺陷**：总喷水外生下，纯阀位阶跃=级间再分配（K≈−0.07），控制器无权威——真实阀门→总喷水链缺联动

## §6.2 修复链（每步预注册门槛）

| 步 | 内容 | 结果 |
|---|---|---|
| FIX1 | 残差去 v1/v2/W：qna 3.04 符号修复但精度回退；**qnaw（去 v1/v2 留 W）2.571 定案**——去伪相关特征反而提升泛化，优于 qh 2.816 与 v2 2.93 | A1/A3 PASS，A2 边缘 FAIL(3.042→qnaw 追回) |
| FIX2 | 固定滞后 τ_sw=60s/τ_sens=15s | B1/B2 FAIL——W 不联动时形态度量无意义，判定前置 FIX3 |
| FIX3 | **可学习滞后**（用户要求：prior×softplus，学得 **τ_sw 73-86s / τ_sens 19-24s**，数据确认滞后是真实结构）+ 全动作屏蔽 qnal + 分状态 W 拟合（湿 R²=0.425 k_w=3.22 / 干 k_w clip 4.0） | A1'/A2'/A3' PASS；湿闭环形态逼近 exp_099（锚点 0.53/0.64/0.71/0.81/0.88/0.93 vs 实测 0/0.10/0.17/0.49/0.70/0.97）；B1' FAIL 干态仍失控 |
| FIX4 | **实测执行机构动态进环**：指令→阀位拟合 τ_a≈883s、速率限制 0.14%/s（R²=0.978）——真实减温阀极慢，是干态环路的真稳定器 | D1/D2/D3 全 PASS，干态阻塞解除 |

**两发现（论文级）**：① 学习组件（残差）会从观测数据学到动作的非因果关联，必须与动作变量隔离，动作通道因果性只能靠物理结构保证；② 瞬时残差学 10s 级快波动、抵消对象 ~600s 热惯性——**跟踪精度与控制保真度是内在权衡**。

## §6.3 TF 提取（STEP6 → STEP6b）

- STEP6 FOPDT×6 工况点拟合 R² 0.64-0.84，二阶 fallback 更差（τ1 钉网格下界）——模型阶跃响应**含过冲**，单调滞后族结构上表达不了（T1 FAIL；T2 符号/T4 级联闭环 6/6 收敛 PASS）
- **STEP6b 精确线性化 PASS（L1/L2/L3）**：autograd Jacobian → 14 维局部状态空间（h×3, Tm×3, rB, Dsw_lag×2, T_sens×5），W 联动内嵌。6 工况点：L1 LTI vs 非线性阶跃 R² **0.990-0.996**；L2 级联闭环（PI+执行机构+LTI）6/6 收敛；L3 特征值 0.966-0.975 < 1 局部稳定

**最终 TF 包（控制嵌入内模）**：`阀门指令 → [执行机构 1/(883s+1) + 速率限制 0.14%/s] → 阀位 → [逐工况点 LTI 增益调度表 (pm 15.6-24.2)] → 温度5通道`；W 联动内嵌 k_w 湿 3.22/干 4.00。路线 A（MPC 替代 PID，离线可验证）内模就绪；路线 B（前馈调 SP）需在线验证——与 CFE"阀位是唯一离线可验证通道"结论一致。

## §6.4 残留局限（如实记录）

- qnal rollout 2.969 与 v2 黑盒 2.93 打平（P1 2.48 未翻）；点wise 优势仅 qnaw（无滞后，2.571）
- 段0 两相欠热未修（sh1_out 首步 21-25°C，中间输出湿态仍差）
- 真实干态 SP/阀位事件各 n=4，样本不足作金标准——干态闭环判定到"收敛且形态合理"为止
- 单窗口单机组；执行机构 τ_a 883s 为一阶近似（真实为速率限制主导的非线性）
- 60s 锚点模型 0.12-0.53 vs 实测 0.00——喷水快径仍在，闭环形态同族但前段偏快

## §6.5 审计修正（2026-08-17，执行方自审计+消融，commit 534cb94）

1. **G1 复测（原门槛：各锚点 |Δ|≤0.15，600s≤0.10）**：FIX4 最终模型按原门槛重算 → **G1 未过**。rate-only 湿态 Δ=[0.12, **0.24**, **0.38**, **0.25**, 0.13, 0.06]（前中段超）；真 τ_a=883s 湿态 Δ=[0.03, 0.03, 0.06, **0.28**, **0.40**, **0.54**]（中后段超）。此前"形态逼近"的表述实为"收敛 ✓、形态未达门槛"。
2. **FIX4 τ_a 生效 bug**：16_fix_actuator.py 的 use_tau 上界 `0.5<τ_a<120` 拒绝实测 883s → 静默回退 τ_a=5s，FIX4 闭环实际只测了 rate-only 动态（τ_a 从未进环）。D 门判定不翻转（只测收敛），但叙事修正：**干态稳定器 = 速率限制（0.0137/步），真 τ_a 进环使湿态过度迟缓**。修正归档 out/fix4_correction.json。
3. **执行机构表征敏感性**：rate-only 干态 tail_std 0.051 贴门槛，速率限制 ±2% 即翻转——干态收敛对参数敏感，未做鲁棒性验证。
4. **qnaw 种子数**：PAPER_MATERIALS 曾误写 3 seeds，实际 2 seeds；20_qnaw_seed2.py 已补 seed2 → 3 seeds **2.570±0.213**（2.784/2.358/2.569），W1（≤3.0）/W2（符号：湿 −0.366/干 −0.19）双 PASS——2-seed 均值有代表性，核心 claim 稳固。
5. **执行机构"指令"信号身份未核实**（PI 输出 vs 运行人员设定）——883s 的物理归属（执行机构/指令平滑/PI 动态混合）仍待 DCS 配置对照。
6. **执行机构身份诊断（21_actuator_identity.py，用户质疑驱动）**：① 指令 99.1% 步不动、无一步 ≥3%、自相关 0.998、与温度偏差相关 0.61——"二级减温调节阀设定"是**慢指令信号**（PI 输出或运行设定），不是执行机构输入；② 0.5% 门槛小步事件（湿 n=3）：阀位 **10s 内到位 63%**——**真执行机构很快，883s 完全不是执行机构硬件**；③ 湿/干分层拟合 817s/504s——**用户假设证实：干湿不是一个 lag**（但两者都是指令动态）。**FIX4"执行机构 883s 稳定干态"叙事作废**：883s≈指令生成动态（PI Ti=350s 量级），塞进闭环=与环内 PI 重复建模；干态闭环稳定性问题**重新打开**，真实稳定来源=PI 自身慢动态+对象热惯性（模型缺失，回到修复 C 方向）。可用工程约束保留：实测阀位速率上限 ~0.14%/s（来源未定但真实）作仿真约束可重现实测闭环形态。

## §6.6 物理本质回归：蒸发干燥动力学 + qnav 平衡突破（2026-08-17 深夜，commit 51f20f7→52449a6）

**FIXB 蒸发干燥（26_fix_evap.py）**：液滴状态 m_liq（dm/dt=Dsw−m/τ_evap）+ 壁面供热 q_wall=α_w·(Tm−Tsat)·(1−dry) + 干燥度门控输出。学得 τ_evap=6.2s（先验 15，数据说液滴蒸发更快）、aW1/aW2=115/289 kW/K、m_dry0=54kg。
- **B1 PASS**：湿态 sh1_out 首步偏差 21-25°C → **3.07°C**——两相输出病灶第一次被物理本体修复
- B2 FAIL：湿态 τ63=1030s 过慢（蒸发缓冲+壁面供热=物理负反馈，天然慢动态过头）
- B3 FAIL：rollout 11.33（湿态 6.01 大幅改善；干态 15.37 未动）
- B4 PASS：干态 K=−3.02 符号对，蒸发结构干态正确退化

**qnav 突破（27_fix_evap_residual.py，fcfb058）**：qnaw 残差（无动作特征）上 e0-evap 底座——"物理慢+残差快=中间节奏"假设证实，**E1-E5 全 PASS**：
- 湿态 τ63=**480s**∈[240,900]（从 1030s 回落）；rollout **2.463**（项目历史最佳，超 v2 黑盒 2.93）；湿闭环收敛且**前 3 锚点首过 G1**（60s 0.039/120s 0.132/180s 0.119）；两相偏差 2.79°C 保持
- 意义：F2 张力在"物理底座正确 + 残差无动作特征"下翻转成可调平衡——之前所有修复失败的真因=残差在错误底座上干活

**qnavlag 反证（28_fix_qnavlag.py，52449a6）**：加 τ_res=86.6s 残差滞后 → rollout 崩到 11.33（两 seed 完全相同——残差贡献被滞后洗净，退化纯 e0-evap）+ 闭环全段过慢（600s 仅 0.40）。**τ_res 是钝刀，qnav 无滞后即最优**。精度与形态在 τ_res 上是悬崖式权衡，不是斜坡。

**最终配置 = qnav**（e0-evap + qnaw 残差）。剩余局限（用户修正后表述）：
- **G1 中段差异不能证明真实响应**：exp_099 锚点是主仓事件研究的平均归一化响应（非理想操作步、事件级方差未入仓），G1 仿真协议是理想阶跃——300s/420s 的 Δ0.34/Δ0.30 无法与真实事件分布方差区分。G1 降级为"形态同族性检查"（前段 60-180s 通过=不再瞬时响应，有意义的弱证据）
- **干态闭环"范围外"表述作废**：干态闭环控制是已投运现实（机组日常超临界工况 POU107 闭环运行）；本仓多次复现干态闭环收敛（FIX4 rate-only D1 PASS、qgate Q2 tail_std 0.0058）；qnav 配置干态不稳是该配置的干态行为缺陷，不是"干态控制不可行"的证据。正确表述：**模型侧干态控制通道验证受数据限制（事件 n=4），干态控制本身已是投产状态**

## §6.7 Q32 第一性原理收口筛查（Codex 独立审计，2026-08-17 深夜，commit 97ee326/aa04388）

**冻结设计**：5 候选 × 2 冻结折（F0 eval [25000,30000)/F1 eval [35000,40000)，历史评估段 [40000,50000) 封存防过拟合），seed 0，驱动不输出裁决。候选：`evap_only`（无残差）/ `double_w`（qnav 原配置，+z,+z 读 W）/ `h_w`（只注焓，读 W）/ `h_now`（只注焓，不读 W）/ `conservative_now`（守恒型 −z,+z，不读 W）。执行方修 Codex 驱动 2 处签名 bug（executor-side fix 标注），矩阵/折/种子/门槛零改动。

**四个冻结问题的答案**：
1. **double 注入冗余**：double_w 3.39/1.93 vs h_w 3.21/2.27——金属侧注热零精度收益，Codex 怀疑成立
2. **读 W 提供条件预测优势**：h_now 4.22/2.90 vs h_w 3.21/2.27——去掉未来 W 后 RMSE 上升，但这只证明 logged-future W 对条件预测有用，不证明其反事实因果有效。h_now 湿态 valve-only 阶跃 −0.11/−0.19；闭环仅 F0 跟踪干净，F1 误差 0.46°C 且阀饱和 98.5%。
3. **守恒型残差不具竞争力但并非零收益**：conservative_now 5.01/5.29，较 evap_only 5.21/5.77 两折均有小幅改善；净注热残差贡献更大，但不能写成“全部精度都来自净注热”。
4. **闭环折敏感**：湿态与干态都存在失败或饱和；干态 F0/F1 尤其分裂。F0 的 double/h_w 固定 +2°C 末值误差小，但换向 582-589 次，不能称稳定成功；F1 干态全部阀饱和。此前单点闭环结论不能外推。

**对论文叙事的修正**：`double_w` 只能作为 logged-future 条件精度上界；`h_now` 是动作权限更干净的候选，但干态符号与闭环尚未通过，不能提前称物理配置成立。raw artifacts 在 out/qnav_first_principles/（manifest + summary + 10 单元）。

## §6.8 Q32-R 残差反馈归因探针（Codex 独立审计执行，2026-08-18 凌晨，commit af9f205）

**设计**：16 个**全为干态**的点 × 2 路径（valve_only 单阀 / coupled 级联）× 6 模式 × 2 折（F0 eval [25000,30000) / F1 eval [35000,40000)，seed 0，无训练）。六模式：physical（纯蒸发底座，无残差）/ live（残差活）/ replay（回放基线残差输出，切断其对动作后代状态的反馈）/ half（残差输出 ×0.5）/ freeze_ts / freeze_tm。负增益占比表示开阀后末温下降的方向正确率。

**核心结果**：

| 折×路径 | physical | live | replay | half | freeze_ts | freeze_tm |
|---|---|---|---|---|---|---|
| F0 干×阀 | 8/8, −0.036, τ63 1650s | **2/8, +0.029, 0s** | **8/8, −0.037, 1685s** | 2/8, +0.012 | 4/8, +0.004, 840s | 1/8, +0.026 |
| F0 干×级联 | 8/8, −1.62, 940s | 8/8, −0.55, 1115s | **8/8, −1.62, 940s** | 8/8, −0.94 | 8/8, −1.52 | 8/8, −0.64 |
| F1 干×阀 | 6/8, −0.035, 670s | **2/8, +0.016, 75s** | **8/8, −0.035, 600s** | 1/8, +0.191 | 0/8, +1.06 | 4/8, +0.002 |
| F1 干×级联 | 8/8, −0.681, 895s | **2/8, +0.329, 185s** | **8/8, −0.635, 845s** | 1/8, +0.259 | 1/8, +2.27 | 4/8, +0.001 |

**三结论**：

1. **内部机制得到支持**：live 的四组方向正确率为 2/8、8/8、2/8、2/8；replay 四组均恢复 8/8，median 增益与 physical 相差不超过约 0.02°C、τ63 相差不超过 15%。这支持“状态依赖的 residual feedback 改写动作响应”，而不是纯物理喷水通道普遍失效。
2. **half 不能支持通用单调剂量结论**：F0 valve-only 位于 physical/live 之间，但 F1 half 可比 live 更正，说明存在非线性交互和阈值；只能说减半不足以稳定恢复符号。
3. **freeze 不能定位单一载体**：两折均为干态，freeze_ts 在 F0 coupled 改善、在 F1 coupled 显著恶化；freeze_tm 也折依赖。因此当前只能定位到整体动作后代状态反馈，不能归因到某一组温度特征。

**后续方向**：`h_now + shared/replay residual` 是待验证的解决候选，不是已完成配置。Q32-R 只验证干态开环内部机制；下一门禁是湿/干多点闭环，确认共享扰动不会破坏湿态且能改善干态控制。

raw artifacts：out/qnav_residual_feedback_probe/（manifest.json + summary_development.json + 16 点明细）

## §6.9 Q32-S shared-disturbance 配对闭环（Linux 执行 af2b168，Supervisor 审计完成）

16个点已确认是 F0/F1 各湿4、干4；48个模式结果与聚合均复算一致。`shared` 整体接近 `physical`，说明 replay 确实移除了状态依赖 residual 的差分反馈；`live` 在部分点改善跟踪，表明 residual 同时承担预测闭合与稳定化。当前结果**不能全局否定纯物理对象**：低压湿态已有多个点能跟踪；近临界低喷水点主要受动作裕量限制；部分湿/干点则暴露出对象闭合或工况调度不足。

控制器边界：Q32-S 的启发式 PI 每10s直接更新，无死区、滤波和 anti-windup。湿态达标点的高换向更像离散控制极限环，不能单独作为对象模型失败证据。Q32-S 还使用600步定输入 warm-up 与基线差分，消除了绝对初始偏差，因此也不能回答初始化误差。

**Q32-T 已审计**：16 点及聚合可复算。physical/shared 在所有 fold/state/±2% 组方向一致；live 压缩响应且 F1 干态方向仅 25%。deadband/LPF 将湿态换向由数百次降至 9–32 次，anti-windup 将 F1 湿态 shared 的中位误差约 0.242 降至 0.002，但滤波并非所有点都提高精度。180 步历史回放的大误差混合了 rollout 动力学与初始化，故只支持继续验证状态估计，不能单独证明 observer 必要。

## §6.10 Direct WM v1 探索结果与协议失效（2026-08-18）

以下数字是 v1 探索结果，不进入论文主表。审计发现：验证段嵌在训练段内；BetaNLL 已先缩成标量使 horizon 权重失效；checkpoint 按 H5 而非 H18 选择；评测窗口受训练 RNG/早停影响；敏感性只扰动未来动作首点。故 v1 状态为 `protocol_invalidated_pending_rerun`。

**rollout step17（180s）MAE**：F0 0.756/0.682/0.807（均值 0.748）；F1 0.889/0.862/1.003（均值 0.918）。

**敏感性（二级阀位 +10 分位，t12）**：F0 −0.066/−0.063/−0.073；F1 −0.370/−0.045/−0.249。方向全部物理正确（负），幅度协议依赖。

**历史观察（三点，仅用于追踪）**：
1. phase1 旧协议的 0.528 在本协议下**不复现**（≈0.75-0.92）——训练数据量（dev-only vs 全文件70%）与评测窗口（折窗口 vs 文件尾15%）都变了，这正是"跨仓数字必须重测"纪律的实证
2. 敏感性幅度比 phase1 旧值 −0.382 小一个量级（F0 −0.067）——同样协议依赖；方向稳定性（6/6 负）是唯一可跨协议保留的结论
3. v2 已冻结为非重叠 Q32 折、固定 200 窗、H18 selector、持续 ±2% valve-only 响应。它仍是使用真实未来阀位的 conditional oracle，并非可部署预测，也不等同于 Q32 的 valve-flow-W 耦合对象响应。

**v1 对象阶跃探针（37_direct_wm_object_probe.py）尺度错误记录**：v1 探针用 ±0.02 原始分位阶跃，而 Q32-T 口径的 2% 开度 = ±2.0 原始分位——**v1 探针的 0.0001°C 结论量级作废**，正确尺度的对象响应须在 v2 训练完成后用 ±2.0 重测。定性判断（动作通道极弱）与 ±10 敏感性方向证据保留。
