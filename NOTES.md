# Ad-hoc 实验笔记：PINN 拆解思想迁移验证（主汽温建模）

日期：2026-08-15
状态：已完成（ad-hoc，非主线）
目的：验证 EWH PINN 论文（Pandiyan et al., Applied Energy 380:125037）的三件套——物理派生特征、不变量损失、分段自回归——迁移到伊敏主汽温对象的有效性，并拆解每个组件的增益来源。

---

## 0. 实验环境与文件清单（可复现审计）

- 环境：`~/anaconda3/envs/Alloftime/bin/python`（torch 2.11.0+cu130，CUDA GB10）
- 工作目录：`~/.hermes/workspace/adhoc_pinn_features/`
  - `00_explore.py` → `out/explore.json`（数据探查：窗口选择、顺序不变量成立率、方向滞后扫描）
  - `01_experiment.py`（主实验：变体定义/训练/rollout，`--variant` 指定或全跑）
  - `02_plot.py`（rollout 对比图）
  - `03_dirscan.py`（单阀方向重扫）
  - `04_mechanism.py`（顺序违例-误差机制分析）
  - `05_amplitude.py`（shape/幅度指标 + 残差驱动筛查）
  - `06_merge.py` → `data/merged_aug.csv`（A/B 侧 mainT + reheat 合并）
  - `07_figures.py` → `out/figs/fig{1..4}_*.png`（图集，Noto Sans CJK 注册方式见脚本头部）
  - `out/`：`results_{v}.json`、`rollout_{v}.npz`（preds/truths）、`summary_ablation.json`、`model_{v}.pt`、`figs/` 图集
- 复跑：`01_experiment.py`（全变体）约 1 分钟；`03/04/05` 各 <30s。

## 1. 数据与协议

- 数据：`~/Desktop/AI/时序预测/TIME-all-model/TCN-Improved-GRU/data/mainT/A侧主汽温全数据_cleaned_10s.csv`（267,090 行 × 41 列，10s 采样，2025-12-24 起，仅读）
- 窗口：行 70686–120686（50K，约 5.8 天）。选择依据：机组负荷 50K 滚动 std 最大（std=150.6 MW，负荷范围 188.8–613.6 MW，覆盖大幅变工况）→ `explore.json.window_*`
- 切分：连续 30K / 10K / 10K（训练/验证/测试），训练窗口 stride-5 采样（5,989 样本）
- 模型（全部变体一致）：GRU(hidden=32, 1层) + Linear(32,5)；输入窗 60 步（10 min）；输出 5 个沿程温度
  `[一级减温入口, 一级减温出口, 二级减温入口, 二级减温出口, 末过出口(主汽温)]`
- 归一化：z-score，统计量（mu/sd）取自训练段前 TRAIN_N=30000 行，模型外部计算（`build_data` 中的 `info.mu/info.sd`，未单独落盘为 json，复算逻辑见 `01_experiment.py`）
- 训练：Adam lr=1e-3，batch 256，≤60 epoch，早停 patience 8（验证集加权 MSE，主汽温权重 2）；种子 0
- 约束损失（λ=0.05）：L_band=relu 带外（主汽温 p1/p99=[557.75, 572.13]°C）；L_order=沿程顺序对 relu 违例（margin 0.5°C，权重 [1.0, 1.0, 1.0, 0.75, 0.3]，对应成立率 100%/100%/100%/91%/66%）
- 物理特征（V1 系）：spray_cool_1/2=喷水总流量×(段入口−省煤器出口给水温度)；adv_1/2/3=主蒸汽流量×段焓差；heat_intensity=煤量/(蒸汽流量+1)；dspray_6=二级喷水指令 6 步差分
- 评估：①单步 RMSE（teacher-forcing，主汽温 °C）②递归 rollout 1800 步（5h）：5 温度自回归喂预测、外生喂真实值；指标 = RMSE/max|err|/带外占比/顺序违例占比/输入分布漂移|z|
- 变体矩阵（parse_variant）：
  | 变体 | 物理特征 | band | 顺序 | 补充驱动(AUG 13列) |
  |---|---|---|---|---|
  | v0 | | | | |
  | v1 | ✓ | | | |
  | v1x | ✓ | | | ✓ |
  | v0b | | ✓ | | |
  | v2o | ✓ | | ✓ | |
  | v2b | ✓ | ✓ | | |
  | v2 | ✓ | ✓ | ✓ | |
  | v2xb | ✓ | ✓ | | ✓ |
  | v2x | ✓ | ✓ | ✓ | ✓ |

## 2. 探查证据（结论的物理基础）

### 2.1 沿程顺序不变量成立率（explore.json.chain_order_rates）
- T_sh1_in > T_sep_out：**100%**
- T_sh2_in > T_sh1_out：**100%**
- T_main > T_sh2_out：**100%**
- T_sh2_out < T_sh2_in：90.7%
- T_sh1_out < T_sh1_in：66.2%
→ 三个 100% 对是无滞后硬约束的物理依据。

### 2.2 喷水方向约束被闭环数据抹掉（两轮证据）
第一轮（explore.json.spray_direction_6step，用**总流量**）：
- 60s 窗差分，opposite_sign_rate：sh1_out 0.529、sh2_out 0.416（≤抛硬币）

第二轮（03_dirscan.py，用户质疑后，**单阀指令×对应侧出口**，滞后 30–360s 全覆盖）：
- SH1A→一减出口(A)：opposite_rate 0.31–0.37，corr **全为正** +0.25~+0.46（30s 处最高 +0.455）
- SH2B→二减出口(A)：opposite_rate 0.30–0.43，corr +0.15~+0.31
→ 即"喷水指令增→出口温度增"在所有滞后系统成立（应为负相关，实为正相关）。
机制解释：闭环反馈指纹——PID 只在温度上升时增喷水，且扰动（烟气/负荷）持续推温数分钟，喷水是抵消项，统计上扰动主导符号。与主线 MS3-R"动作当场景特征非因果响应"（shuffled→2.12/3.79）同源。
结论：方向损失不可用；顺序约束不受影响。

### 2.3 其他探查
- 阀位-指令相关 0.9993/0.9982 → 阀位列冗余，丢弃
- 主汽温 |ΔT| 分位：p90=0.19、p99=0.41、p99.9=0.70 °C/步（速率有界，未用）
- 沿程温度与主汽温相关性低（|corr| 0.02–0.22）→ 单步预测信息主要在外生变量

## 3. 主实验与拆解结果

### 3.1 拆解表（同一进程、同种子 0；summary_ablation.json）

| 变体 | 单步RMSE °C | rollout RMSE | maxerr | 带外% | rollout顺序违例步% | 漂移\|z\| |
|---|---|---|---|---|---|---|
| v0 | 1.13 | 3.81 | 8.45 | 9.1 | **96.9** | 1.11 |
| v1 | 1.17 | 3.65 | 9.98 | 6.9 | 81.4 | 0.85 |
| v0b | 1.16 | **5.61** | 12.02 | 9.6 | 53.9 | 1.46 |
| v2o | **1.10** | 3.00 | **7.30** | **0.0** | **0.3** | 0.65 |
| v2b | 1.27 | 3.33 | 9.09 | 3.9 | 96.9 | 0.55 |
| v2 | 1.39 | **2.48** | 9.60 | 0.4 | 33.3 | 1.15 |

训练期约束活跃度：band_act v0b=0.78 / v2b=0.72 / v2=0.74（每 batch 平均 72–78% 样本带外违例）；order_act v2o=v2=1.00（**每个 batch 都有顺序违例**，即纯 MSE 模型本能违反沿程顺序）。

### 3.2 机制证据（04_mechanism.py）
- 顺序违例步 vs 正常步的误差：v0 = 3.36 vs 1.47 °C（违例步误差 2.3 倍）
- v0 平台段（step 708+，118–121 min）：sh1_out 475.4 > sh1_in 462.9（喷水段反而升温，物理不可能），sh2_out 510.3 < sh2_in 513.5，整条链坍塌后主汽温锁死 562.5 追不上真实 565+ → 上游链失序导致主输出冻结
- 漂移指标与 rollout 质量不单调（v2b 漂移最小 0.55 但 RMSE 3.33；v2 漂移 1.15≈v0 但 RMSE 2.48）→ "输入分布漂移"假设不成立；起作用的是"是否在物理流形上"（顺序），不是"离均值多远"

## 4. 形状/幅度与缺失驱动（用户观察的验证）

### 4.1 shape/幅度指标（05_amplitude.py；corr=形状保真，std_r=幅度还原比）

| 变体 | corr | centeredRMSE | bias | std_r |
|---|---|---|---|---|
| v0 | 0.264 | 3.63 | +1.13 | 1.158 |
| v1 | 0.281 | 3.17 | +1.80 | 0.912 |
| v2 | 0.582 | 2.48 | +0.06 | 0.959 |
| v2b | 0.438 | 2.52 | +2.19 | 0.585 |

→ 用户观察"v1 形状更好"量化确认（crmse 3.17 vs 3.63），幅度偏置 +1.80°C 确认；v2 形状最好（0.58）且幅度几乎完全校准（bias 0.06, std 0.96）。

### 4.2 v1 残差候选驱动筛查（05_amplitude.py，残差=预测−真实）
| 候选 | corr_level | corr_diff |
|---|---|---|
| 二级减温调节阀设定（控制器内部） | **−0.570** | +0.049 |
| 再热出口汽温 | **−0.519** | −0.196 |
| 高压缸排汽至再热器温度 | −0.449 | −0.294 |
| 再热器一级减温入口汽温 | −0.431 | −0.258 |
| 再热器减温水总流量 | −0.376 | −0.121 |
| 末级过热器出口汽温_B | −0.374 | −0.256 |

→ 用户"再热器/B侧影响幅度"假设获支持；另发现最强候选是控制器内部变量（二级减温调节阀设定）。相关性≠因果（共同负荷/燃烧扰动），diff 相关较弱（−0.1~−0.3）。

### 4.3 补充驱动变量实验（AUG=13 列：A侧控制器设定×4、B侧主汽温+喷水×3、再热侧×6）

| 变体 | corr | cRMSE | bias | std_r | RMSE | maxerr | 说明 |
|---|---|---|---|---|---|---|---|
| v1x (+13驱动) | **0.691** | 3.08 | −0.68 | 1.539 | 3.15 | 7.73 | 形状翻倍，幅度过冲 |
| v2xb (+band) | 0.692 | 2.98 | −0.79 | 1.493 | 3.08 | **7.66** | band 中性 |
| v2x (+band+顺序) | 0.111 | 4.21 | −1.65 | 1.266 | 4.52 | 11.46 | **崩塌** |

→ 补驱动使形状保真度 0.28→0.69（波峰波谷时序结构主要由再热/B侧/控制器变量解释）；但幅度过冲（std 1.54）。顺序约束与补充驱动**互斥**：v2x 中 order_act=1.0（每 batch 打架），val_mse 0.0824 vs v2xb 0.0214——驱动在场时顺序约束变成紧身衣。

## 5. 结论（每条附证据链）

1. **物理组合特征单独加入无益反害**：v1 全面劣于 v0（RMSE 3.65 vs 3.81 的噪声内，但 maxerr 9.98 vs 8.45、带外 6.9 vs 9.1 无优势；首轮独立进程 v1 4.24 更差）。证据：3.1 表 + 4.1 表（v1 bias +1.80 最差）。
2. **不变量损失有效且只在闭环生效**：v2 单步 1.39（全组最差）→ rollout 2.48（全组最好）。机制：单步走 teacher-forcing 只见约束偏置成本；rollout 收益来自顺序约束防止自回归链失序（违例率 96.9%→33%，v2o 0.3%）。证据：3.1/3.2 + 4.1（v2 bias 0.06）。
3. **band 是护栏不是引擎，单独加有害**：v0b rollout 5.61 劣于 v0 3.81（corr −0.34 反相关）。有物理特征+顺序骨架时为正贡献（v2b 3.33→v2 2.48）。证据：3.1 表 v0b 行。
4. **顺序约束自带 band 效果**：v2o 带外 0.0%（主汽温>二减出口锁下限）。证据：3.1 表 v2o 行。
5. **喷水方向约束在闭环数据中不可用**（所有滞后反向相关）。证据：2.2 两轮扫描。
6. **形状与幅度是正交维度，驱动变量给形状、约束校准幅度**：v1x corr 0.69 但 std 1.54；v2 corr 0.58 但 bias 0.06/std 0.96。证据：4.1/4.3。
7. **顺序约束是缺失驱动的替代品而非补充**：v2x（驱动+约束全加）崩塌 corr 0.11。证据：4.3 表 + order_act=1.0 与 val_mse 对比。
8. **再热侧经烟气耦合是主汽温幅度的重要驱动**（残差相关 −0.38~−0.52）。证据：4.2 表。注意相关性非因果性边界。

## 6. 局限与噪声边界（审计必读）

- **单种子（0）、单窗口（50K）、单 rollout 起点（测试段起点）**：所有数字是点估计。
- 跨进程重跑 v1 得 4.24 vs 同进程 3.65（±15% 级）→ RMSE/maxerr 排序在 v1x/v2xb/v2o/v2 之间**不可信**；稳健的是大反差（v0/v1 vs v1x 的形状差、v2x 崩塌、v0b 劣化、v2o 带外归零）。
- 顺序违例指标 order_viol_frac（main>sh2_out 单对）不区分变体（恒 0），机制分析用的是 5 对联合 viol_any。
- z-score 参数未单独落盘（在脚本 info 内），复算需按 build_data 逻辑。
- 残差-驱动相关是水平相关，diff 相关弱，因果性未验证（共同扰动混杂）。
- 未调超参（λ=0.05、margin 0.5、hidden 32 均为固定值）；v2x 崩塌未做 λ 扫描确认。

## 7. 对后续工作的启示（记录用，主线未动）

- 特征优先级：真实驱动变量 > 物理组合特征 > 结构不变量（当且仅当驱动缺失）。
- 对闭环 rollout 用途模型：约束加在自回归状态结构上（沿程顺序），而非输出数值区间。
- 任何"物理方向"先验在闭环数据上必须做滞后扫描验证（本项目两轮扫描的教训）。
- 可迁移到主线世界模型的候选：C31 递推闭合问题可用"状态结构约束"思路探索——仅记录，未经主线评审流程，不视为授权。
