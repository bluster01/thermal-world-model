# 论文素材清单 — 灰盒+残差世界模型的控制嵌入链 (adhoc2, 2026-08-17)

证据链全在仓库提交历史 (606823a → f1a3922), 所有数字可复现 (本机 torch 定种子 bit 级)。

## F1 发现: 残差动作污染 (learned-residual action pollution)

**现象**: 灰盒+残差模型 (qh, rollout 2.82 超黑盒) 在开环反事实测试中阀门阶跃符号翻转 — 湿态 v2+5% 使主汽温 +0.073°C (开阀应降温)。

**机制**: confounding by indication。闭环数据里阀门动作总与扰动并发 (PI 响应偏差/运行人员搬 SP/负荷变动), 残差 MLP 拿到动作特征后学到 "v↑ → 注热" 的非因果捷径 (训练损失更低); 开环反事实注入时捷径触发, 盖过物理喷水效应。灰盒本体免疫 — 阀门通道是方程结构保证的因果 (质量守恒+焓混合), 不依赖学习。

**证据链 (三步对照, 单实验):** e0 纯灰盒 −0.047 ✓ → qh 残差带v +0.073 ✗ → qnaw 残差无v −0.191 ✓。且去伪相关特征后 rollout 精度不降反升 (2.816→2.571) — 残差在动作特征上浪费容量。

**落点**: 混合 (physics + learning) 建模中学习组件的因果隔离设计原则; 反事实阶跃测试作为控制用模型的必备验证; "动作屏蔽"作为混杂控制手段的实证。

## F2 发现: 跟踪精度与控制保真度的内在张力

**现象**: 点wise RMSE 2.57 (超黑盒 v2 2.93) 的模型, 开环阶跃 τ63=10s/θ=0, 闭环响应比实测快 2-4× (干态 60s 处 0.42 vs 实测 0.00)。

**机制**: 瞬时残差为追踪 10s 级数据快波动而学, 训练目标天然要求它抵消物理热惯性 (~600s) — 对预测最优的解对控制设计有害。

**证据**: ① Step⑤ G1/G2 FAIL; ② 修复 = 可学习滞后 (数据自己确认结构: τ_sw 73-86s / τ_sens 19-24s, 先验 60/15) + 实测执行机构 (τ_a≈883s, 速率 0.14%/s — 干态环路的真稳定器); ③ FOPDT/二阶拟合对阶跃响应全部 R²<0.85 — 残差+滞后复合结构的响应含过冲, 单调滞后族表达不了。

**落点**: RMSE 域与控制可用性不单调; 控制用模型必须在动态响应域验证; 残差动态化 (滞后残差/导数惩罚) 是可行的权衡调节器。

## F3 方法贡献: 动态响应域验证协议 (便宜且决定性)

- 差分阶跃 (step − baseline, 外生恒定于工况点) + 真实 PI (POU107 反作用 PI, FX 调度表) 闭环 + 实测锚点曲线 (exp_099: 60s 0/120s 0.10/180s 0.17/300s 0.49/420s 0.70/600s 0.97)
- 每步预注册门槛, 边缘 FAIL 如实记录不豁免 (FIX1 A2 3.042 vs 3.0)
- 成本: 2 小时跑出三病灶 (动作污染/无时间结构/执行机构缺失) — 比点wise 优化一个数量级的信息量

## 系统管线 (架构图素材)

```
观测数据 → 灰盒 (焓+质量守恒, 24 参数, 硬约束) ⊕ qnaw 残差 (动作屏蔽, MLP 11特征)
→ 可学习滞后 (τ_sw 73-86s 喷水输运 / τ_sens 19-24s 传感器)
→ W 联动 (分状态 k_w 湿3.22/干4.00) → 实测执行机构 (1/(883s+1) + 速率 0.14%/s)
→ 增益调度 LTI (6 工况点, 14 维局部状态空间, autograd 精确线性化 R² 0.990-0.996)
→ 控制内模 (路线A MPC 替代 PID 离线可验证; 路线B 前馈调SP 需在线)
```

## 数字卡片 (写作引用)

| 指标 | 值 |
|---|---|
| qnaw rollout (3 seeds) | 2.571 vs qh 2.816 vs v2 黑盒 2.93 |
| qnal rollout | 2.969 (滞后代价 ~0.4, 换来动态保真) |
| 学习滞后 | τ_sw 73-86s / τ_sens 19-24s (先验 60/15, 未退化) |
| 执行机构 (实测) | τ_a 883s, 速率 0.14%/s, 拟合 R²=0.978 |
| 线性化保真 | R² 0.990-0.996 × 6 工况点 (LTI vs 非线性) |
| 湿态闭环 vs 实测 | 0.12/0.33/0.54/0.74/0.83/0.91 vs 0/0.10/0.17/0.49/0.70/0.97 |
| 级联闭环收敛 | 6/6 工况点 (norm600≈1.0, tail_std≤0.01) |
| 局部稳定 | 特征值 0.966-0.975 < 1 |

## 已知弱点 (审稿人视角, 提前备好)

1. 单窗口单机组 (训练 3.5 天 / 测试 5.8 天), 无跨机组泛化 — 单机组需重辨识
2. 点wise 优势仅在 qnaw (无滞后); 控制配置 (qnal) 与黑盒打平 — "精度"叙事要换成"控制可用性"叙事
3. 干态闭环判定到"收敛且形态合理", 真实干态事件 n=4 不足作金标准
4. 段0 两相欠热未修 (sh1_out 首步 21-25°C) — 中间输出湿态监测仍不可靠
5. 执行机构 τ_a 883s 为一阶近似 (真实为速率限制主导的非线性)
6. 60s 锚点偏快 (0.12-0.53 vs 0.00) — 喷水快径仍在, 高频段保真不足

## 可能的论文结构 (三选一)

- **A. 方法论文**: "Physics-residual hybrid world models for control: causal isolation, learnable lags, and exact linearization" — F1+F2+F3 为贡献
- **B. 应用论文**: 火电机组主汽温灰盒建模与控制嵌入 — 以管线+验证协议为主体, 两发现作为讨论
- **C. 短篇/会议**: F1 单独成文 (残差动作污染+反事实阶跃测试) — 机制干净、证据链紧凑, 最快落地
