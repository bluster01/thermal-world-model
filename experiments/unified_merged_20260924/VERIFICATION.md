# 合并表入口软件验证

2026-09-24：16项数据合同合成测试、7项新runner合成回归测试通过。

新runner与`d14a5a80`原runner只有四处导入调整：复用原模型和汇总，调用本批数据核验。任务、模型、种子、训练超参数、45次完整矩阵及评测顺序逐项核对一致。

数据合同取自Linux `d4a04eb` 已建包的18个文件SHA256及80个来源身份；核验包含读取布局、目标配置和归一化有效性。Linux启动时核对其实际数据文件。此处测试只使用合成小数组，尚未执行工业训练。

原发布包的45项软件测试记录保留于`../unified_experiments_20260923/VERIFICATION.md`。新数据入口直接消费Linux现有包，使用独立实验标识与输出目录。
