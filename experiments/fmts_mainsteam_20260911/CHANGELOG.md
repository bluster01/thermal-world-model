# Change log

## 2026-09-11 — GitHub release

- User authorized push. Release assembled on origin/main at e394702 in an
  isolated worktree, preserving seven upstream commits and unrelated local work.
- Included final synthetic verification bundle (four arms, eight replayed arrays).
- Linux training remains pending; repository publication does not start a run.

## 2026-09-11 — revision 0.2 implementation complete

- Connected 23 historical channels through explicit rich-history adapters.
- Added shared quality-filtered manifests, training-only normalization, all four
  model arms, frozen selectors, raw prediction/persistence/response exports.
- Added seed summaries, day-block paired statistics and supported-only sensitivity.
- Added IAPWS/record identity gates, run ledgers, failure retention and read-only
  artifact/numerical replay audit. Full runner rejects output overwrites.
- 213 final_wm tests passed; four-arm synthetic training and eight-array-set
  checkpoint replay passed. State is ready_for_linux; formal training not started.
- Updated README/TODO and RUN_LINUX.md. Original v0.1 and intermediate smoke
  evidence remain historical; no v0.7 scientific verdict changed.

## 2026-09-11 — revision 0.2, author-requested rich history

- Registered all nine canonical v2.2 boundary extensions as past-only features
  for the blackbox and both hybrid observers: 23 historical variables total.
- Pure greybox retains its physical feature subset; effective feature access
  differences must be disclosed in the family comparison.
- Previous implementation tests apply to revision 0.1 only. Revision 0.2 input
  plumbing and the full runner remain pending; Linux readiness remains false.

## 2026-09-11 — protocol v0.1 implementation started

- Author approved preparing the tokenized cross-attention observer experiment.
- Scope frozen to replacing the initial-state observer only.
- Kept the physical transition, action path, action-blind closure, boundary
  model and anchor-relative correction contract unchanged.
- Preserved GRU as the default and kept its historical state-dict keys.
- Registered 16-step patches, stride 8, one attention layer, four heads and
  state-query cross-attention for the candidate.
- Separated validation-only observer selection from the final three-family
  comparison. Response-curve appearance is explicitly not a selection metric.
- Did not open canonical data or the locked chronological test split; did not
  start full training.

## 2026-09-11 — local implementation checkpoint

- Materialized `frozen_spec.json` after the author-approved freeze.
- Synthetic smoke audit passed anchor identity, correction bound, finite
  rollout, identical-input rollout identity and unchanged non-observer configs.
- Focused experiment tests passed 43/43; the complete `tests/final_wm` suite
  passed 206/206.
- Existing `t1_closure_cons_norew_seed0.pt` loaded strictly with all 82
  state-dict keys, confirming default-GRU backward compatibility.
- Preserved `linux_ready=false`: the matched-manifest runner and raw-array
  exporter still require implementation and verification before dispatch.
- No canonical data, locked test, full training, commit or push occurred at
  this checkpoint.
