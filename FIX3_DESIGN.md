# FIX3 设计稿：A(残差全动作屏蔽) + B(可信W子模型) + 可学习滞后

日期: 2026-08-17 | 状态: 预注册冻结 | 依赖: 修复①qnaw / ②qlag / ③W联动的结论

## 1. 目标

同时落地三项:
- **A**: 残差输入去掉全部动作变量 v1/v2/W (10特征, qna配置) — 动作通道彻底免疫 (修复③发现 W 在残差特征里仍会在干态污染)
- **可学习滞后**: τ_sw (喷水输运) 与 τ_sens (传感器) 从固定工程先验改为可学习参数
  - 参数化: τ = prior × softplus(raw), prior τ_sw=60s / τ_sens=15s, raw 初始 ln(e−1)≈0.5413 → 初始=先验
  - 与残差联合训练 (同一 Adam), 通过 60 步积分反向传播
  - 审计 (B2'): 学得 τ_sw ∈ [10,600]s, τ_sens ∈ [5,120]s — 越界=学习滞后假设被证伪的标志
- **B 可信 W 子模型**: 分状态 (wet/dry) 偏回归 W ~ v1 + v2 取 v2 系数 → k_w_state = clip(系数/W均值, 0.5, 4.0); 报告每状态 R²/样本数 (低 R² 如实记录)

## 2. 预注册判定 (冻结, 不可改)

- **A1'**: 湿+干 耦合阶跃 (v2+5% + W按k_w_state联动) main 末值均 < 0 — 残差全屏蔽下动作通道终极免疫
- **A2'**: rollout main rmse 2seeds mean ≤ 3.2 (qna 3.04 + 滞后容差)
- **A3'**: 湿态耦合闭环 norm600 ∈ [0.8,1.2] 且 tail_std ≤ 0.05 (收敛不振荡)
- **B1'**: 干态耦合闭环 norm600 ∈ [0.8,1.2] 且 tail_std ≤ 0.05 (修复③的失控点)
- **B2'** (审计, 不设硬门槛): 学得 τ_sw/τ_sens 值 + 是否越界, 报告

裁决: A1'∧A2'∧A3'∧B1' = PASS。B2' 越界 → 记录为"可学习滞后退化"发现, 不翻转裁决。

## 3. 产物

out/fix3_learnlag_summary.json, out/figs/fig18_learnlag.png, model_res_qnal_seed{s}.pt
