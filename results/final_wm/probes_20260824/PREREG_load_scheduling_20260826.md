# 变负荷参数调度 — 改动优先级预注册（execution-side, 2026-08-26）

> 预注册纪律：本文件在**跑臂之前**写定假设、实现点、主指标与判定阈值。跑完只填结果，
> **不允许事后改判据**。任何偏离必须在"偏离记录"节显式登记。
> 执行侧口径：不写 verdict、不改 matrix_summary、不动 `experiments/final_wm` 树。

---

## 0. 共同协议（所有臂）

| 项 | 取值 |
|---|---|
| 记录 | `canonical_sideA_v1fixed.npz`（v2.1 接线，n=707,709） |
| 预算 | epochs=120 / patience=20 / batch=32 / bpe=200 / lr=1e-3 |
| 边界 | oracle；history 96 步；horizon 18 |
| 闭包 | `conservative_norew`（T1 判决 SUPPORTED +5.7/+6.9%） |
| 物性 | **GridThermoProperties**，`probe_guard.assert_grid` 前置 + `verify_ledger_properties` 回读 |
| 主指标 | **H18 MAE, ch4 = final_outlet_temp**，256 窗 seed50k，sideA val |
| 副指标 | Q1-Q5 分箱极差；`step_response_direction` 的 `frac_negative` |
| 种子 | 先 seed0；晋级后补 seed1/2（管线固定种子逐位可复现，同种子对比有效） |

### 判定规则（三档，事前锁定）

> **2026-08-27 强制修正（外部审计命中）**：本节原写的方向硬门 `frac_negative = 1.000` 是
> **已废弃的 v0.2 口径**。冻结协议 **v0.3（2026-08-20 生效）§5.1** 明确将其判为"标定错误"：
> 原"100% 窗口负响应"**严于真实对象可观测行为**——本地协议化事件研究实测真实对象 v2
> 60 步正确方向占比仅 **0.68（up, n=22）/ 0.75（down, n=48）**。修订后判据为：
> **均值终端 ΔT < 0 且 day-block bootstrap CI 整体低于 0 且正确方向占比 ≥ 0.60**，
> 且 **H18/H60 两档同报**。
>
> **由此推出两条对本轮结论的修正**：
> 1. 本轮 `direction_v2_test.py` 报告的"8 组 frac_negative = 1.000，方向持证成立"**用错判据**：
>    未算 day-block bootstrap CI、未报 H60。该结论降级为"点估计方向一致"。
> 2. **更严重**：我们的模型达到 1.000，而**真实对象只有 0.68-0.75**。模型方向一致性
>    *高于*真实对象，这是**过度确定性/过阻尼**的可疑信号，不是优点。本轮把它当作
>    正面证据报告，属误读，撤回该表述。

- **晋级（PROMOTE）**：ΔH18 ≤ **−5%** 且 分箱极差恶化 ≤ **+10%** 且 **方向门（v0.3）通过**：
  均值终端 ΔT < 0、day-block bootstrap CI 整体 < 0、正确方向占比 ≥ 0.60，H18/H60 同报
- **否决（REJECT）**：ΔH18 ≥ **+5%**，或方向门（v0.3）不过
- **不确定（INCONCLUSIVE）**：−5% < ΔH18 < +5% → 记录为"该结构对 H18 无收益"，
  价值须转到外推/H60/可解释性上另行论证，**不得改写成正收益**

### 种子纪律（同一审计命中）

冻结协议 J1 条明文：**"单 seed/单 fold 结果不产生路线冠军（MS5 纪律）"**。
本预注册全部数字（基线 0.4840、LPV 三臂、A1）**均为 seed0 单种子**，
seed1 于 ep37 被人为终止、seed2 未运行。
→ **所有判定一律标注为"单种子探索性"，在补齐固定三种子前不得作为路线判决引用。**

### 基线（对照锚点，均为同协议 seed0 实测）

| 臂 | H18 | 分箱极差 | val_nll |
|---|---|---|---|
| 修正记录无锚（**主基线**） | **0.4840** | 1.13× | 1.1096@86 |
| + LPV 自由 τ 调度 | 0.4666 | 1.37× | 1.0956@86 |
| 旧记录无锚 armA | 0.723 | 1.69× | 1.2585@51 |
| 旧记录+锚定 armC | 0.478 | 1.74× | 1.0994@51 |
| 黑箱 iTransformer | 0.354 | — | — |

---

## 1. 优先级表（事前排定）

| ID | 改动 | 文献依据 | 调度变量 | 成本 | 状态 |
|---|---|---|---|---|---|
| **A1** | UA ∝ ṁ^0.8，**零自由参数** | Dittus-Boelter (Nu=0.021Re^0.8Pr^0.4) | ṁ | 2.5h | 已排队 |
| **A2** | `initial_state_mode='steady'` 消融 | 自家 O1 判决（learned MIXED/hybrid REJ，v0.2 老预算） | — | 2.5h | 本次预注册 |
| **A3** | **多项式调度 vs 幂律调度** | Fan2021 Eq.24（三次最优，四次发散） | ṁ | 2.5h | 本次预注册 |
| **A4** | **k（燃料→吸热增益）按负荷调度** | Fan2021 Eq.24 的 k₁ | ṁ（负荷代理） | 2.5h | 本次预注册 |
| **A5** | 水煤比 / 能量失配修正 | Fan2020 修正因子(uB/Dfw)、Fan2021 Eq.4/6 | D/uB, (D−a·rb) | 2.5h | **条件化于 A4** |
| **A6** | 压力作第二调度变量 | 本项目实测偏相关 +0.95 | (ṁ, p) | 2.5h | 本次预注册 |
| **A7** | 压降二次多项式替换线性插值 | Fan2020 g₁(pm)/g₂(pm) | pm | 2.5h | 低优先 |
| **A8** | 分段阶次自适应 | Cui2022（低负荷阶次上升） | — | 结构性 | 最后 |

---

## 2. 逐项预注册

### A1 — UA ∝ ṁ^0.8（零自由参数）

- **假设**：负荷相关的慢动态来自金属热惯性 Cm/UA，而非蒸汽输运延迟。
- **三方证据**（事前已具备）：实测 Q1/Q5 峰值滞后比 **2.33**；Dittus-Boelter 零参数预测
  (492/177)^0.8 = **2.26**；文献经典 600 MW SST 模型 50%/100% 时间常数比 **2.2**。
  密度反证：ρ 随负荷 32.3→74.4 kg/m³，ρ/ṁ 只预测 **1.21**（误差 48%）→ 输运不是主导。
- **实现**：`FlowScheduledTransition` 中 α_τ 冻结为 0、α_UA 冻结为 0.8（已验证：τ 因子恒
  1.000，Cm/UA 比 2.26）。
- **预期**：若 PROMOTE，则"自由 τ 调度学到的 α_τ=1.35 是金属热惯性的代理"成立；
  若与自由 τ 臂打平，说明两条路径**冗余**，应选零参数版（可辨识性更优）。
- **反证条件**：若显著差于自由 τ 臂（≥+5%），则传热代理假说不成立，需回到输运侧另找解释。

### A2 — 纯物理初值消融（量化"神经网络买到多少"）

- **假设**：观测器的学习式初值修正对 H18 有实质贡献。
- **反向证据（事前）**：自家 O1 判决为 **learned MIXED / hybrid REJECTED**（hybrid 3/3 CI 过零），
  但那是 **v0.2 老预算 + 旧错侧记录**下的结论，需在 120/20 + v2.1 记录下复测。
- **实现**：`initial_state_mode='steady'`（纯 `initial_steady_state()` 物理解），其余不变。
- **预期方向**：**不预设**。若 steady 与 hybrid 打平 → 论文必须如实写"NN 状态修正无精度增益"，
  神经网络的价值只保留在闭包（+6%）、联合辨识（J1 +13-33%）、概率化输出三项上。
- **这条是诚实性检查，不是找增益**：结果为负也必须写入论文。

### A3 — 多项式调度 vs 幂律调度

- **假设**：调度的**函数形式**不是关键，可辨识性才是关键。
- **实现**：把 τ/UA 的调度从 θ·s^(±α)（2 参数、指数有界、符号钉死）换成
  θ·(c₀+c₁L+c₂L²+c₃L³)（Fan2021 形式，4 参数/被调参数，正性用 softplus 保护）。
- **预期**：打平（INCONCLUSIVE）。**这正是我们想要的结果**——它支撑"灰盒价值在结构约束
  下的可辨识性，不在拟合能力"，与 Fan2021 四次发散互为佐证。
- **若多项式显著更好**：说明幂律形式过于受限，需重新设计调度族（并在论文中如实说明）。
- **若多项式发散/显著更差**：复现 Fan2021 的四次发散现象，作为"表达能力有害"的直接证据。

### A4 — k 按负荷调度（Fan2021 Eq.24 的 k₁ 类比）

- **假设**：燃料→金属吸热增益 k 随负荷变化（低负荷燃烧恶化、炉温下降、效率下降）。
- **文献**：Fan2021 明确用负荷三次多项式调度 k₁；物理理由见其"nonlinearity"段。
- **实现**：子类覆写 `k_of(pm)`，改为 `k_of(pm, L)`，在现有压力干湿混合之上乘一个
  有界负荷因子 `exp(tanh(w·(L−1))·ln k_max)`，k_max=1.5（±50% 带内，不越物理规则）。
  当前 `k_of` 已是 (B,3) 输出，接口天然兼容。
- **事前定量支撑**：用单一常数水煤比 a=4.485 时，能量失配分箱均值从 **−43.1（Q1）到
  +107.2（Q5）t/h** 单调摆动 → 存在系统性的负荷相关热输入偏置。
- **冗余警告**：A4 与 A5 **经实测判定为不冗余**（水煤比仅 52.5% 方差由负荷解释），
  两臂独立评估；若均 PROMOTE 再跑联合臂验证是否叠加。

### A5 — 水煤比 / 能量失配修正（**独立臂，非条件化**）

> **2026-08-26 偏离登记**：本条原写为"条件化于 A4、预期 INCONCLUSIVE"，依据是我用
> `主蒸汽流量/未校正总煤量` 拼的代理（a=4.485）。改用 **DCS 真实水煤比测点**（全量
> 381 列第 371 列）后该判断不成立，故升级为独立臂。原代理值 4.485 亦为错值。

- **文献**：Fan2020 把吸热修正因子建在 uB/Dfw 上；Fan2021 用
  `Q₁ = k₁·rB + [m/(Ne+g)]·(Dfw − a·rB)`，失配影响在高负荷减弱。
- **DCS 真实测点**（`~/Desktop/AI/时序预测/AA数据中心/伊敏12.10/cleaned_data/all_merged_10s.csv`）：
  - 371 列 `水煤比`（tag 均值 **3.662**；用 247 列给水 / 373 列校正后总燃料量 重算 3.524）
  - 247 列 `选择后的主给水流量`、373 列 `校正后总燃料量`、248 列 `未校正总煤量`
  - 筛选运行工况（MW>160、1<水煤比<8、燃料>50）后 n=1,132,608（95.0%）
- **事前实测（三条，均与文献或我早前推断相左）**：
  1. **方向相反**：`corr(负荷, 水煤比) = +0.722`，我们机组水煤比随负荷**升高**
     （Q1 3.563 → Q2 3.472 → Q5 3.912），Fan 2021 是随负荷**下降**而升高
     → 机制成立但**系数与单调性不可移植**；
  2. **非单调**：Q2 (211-264 MW) 是最低点，Q1 回升 → 与 Fan 所述"低负荷需多给水保护
     管壁"机制**部分吻合**，两种机制（高负荷效率 vs 低负荷保护）在 Q2 附近交界；
  3. **水煤比不是负荷的函数**：负荷二次拟合仅解释 **R²=0.525**，残差 std=0.151
     （占均值 **4.13%**），箱内散布 2.83-4.84%
     → **将近一半方差独立于负荷**，是独立调度变量而非负荷的重复表达。
- **由此推出**：A4（负荷调度 k）与 A5 **不冗余**，须各自独立评估；若两臂都 PROMOTE，
  再跑联合臂检验是否叠加。
- **实现（Linux 运行前冻结细化，2026-08-28）**：`metal_power` 通道注入
  `30000·tanh(tanh(w_raw)·z_wc)` kW，三段等分；`z_wc=(wc−wc_ref(L))/σ_train`。
  其中 wc 取真实测点、wc_ref(L) 为仅用 train split 拟合并冻结的负荷二次基线；
  `tanh(w_raw)` 有界且符号由数据学（**不预设**），总修正硬界 30 MW，`w_raw=0`
  严格退化为原模型。该细化不改变原假设，只把原“w 有界”落实为可验证的量级契约。
- **数据前置**：canonical v2.1 边界不含水煤比，需先把 371 列并入记录（对齐口径同
  `build_canonical_v2`），此为 A5 的**前置数据工作**，成本计入。
- **当前状态（2026-08-28）**：数据契约与独立探针已冻结为 canonical v2.2，等待 Linux
  依次执行 build → sanity → seed0 train/eval；正式 matrix 与历史判决不动。
- **匹配对照修订（Linux 运行前，2026-08-28）**：运行工况门会改变训练/验证样本，故旧
  0.4840 不能单独作为因果对照。先在完全相同的 v2.2 有效窗口上、以相同 seed/预算训练
  `w=0` 对照，再训练 A5；−5% 与分箱 +10% 均相对该匹配对照计算。旧锚仍并列报告。
  这是消除样本筛选混杂，不增加结构臂或搜索自由度。

### A6 — 压力作第二调度变量

- **假设**：纯流量调度在压力与流量解耦的工况下误配。
- **事前证据**：控制流量后，压力对停留时间代理的**偏相关 +0.954/+0.956**；
  固定负荷下压力仍跨 14.0-19.5 MPa（Q3 箱内 12.9-22.5 MPa，散布 10.9%）；
  **LPV 自由 τ 臂唯一恶化的箱正是 Q3（0.503→0.568）**，与预测位置吻合。
- **实现**：调度因子扩为 `s_ṁ^(−α_τ) · (p/p_ref)^(−β_τ)`，β 同样 sigmoid 有界、符号钉死。
- **判定补充**：本臂**额外要求 Q3 分箱不恶化**（这是它的靶点），否则即使总体 −5% 也记为
  INCONCLUSIVE。

### A7 — 压降二次多项式

- **文献**：Fan2020 `g₁ = 0.01784pm² − …`、`g₂ = 0.04112pm² + 1.867pm + 10.0919`。
- **现状**：我们用 `p₀ = pm + (p_out−pm)/3`、`p₁ = pm + 2(p_out−pm)/3` 线性插值。
- **风险**：p₀/p₁ 进入 Tsat 与温度反算，改动会影响所有观测通道，**不是局部改动**。
- **优先级低的理由**：未有本项目实测支撑（不像 A1/A6 有数字），且 Fan 的系数不可移植。

### A8 — 阶次自适应

- Cui2022 指出低负荷惰性段**阶次上升**（不只是参数变）。我们固定 3 段。
- 结构性改动，成本高、与 v0.6 其他工作冲突，**排最后**。

---

## 3. 与 v0.6 既有优先级的关系

对侧 v0.6 已排定：①再热汽温作边界 ②分级减温水流量测点。二者与本表**不冲突且互补**：
- ① 补的是**未观测热输入**（本表 A4/A5 试图用负荷/水煤比间接代理的东西，①是直接测量）；
  故 ① 若落地，A4/A5 的价值会下降 —— **预注册记录此依赖**。
- ② 补的是**分级喷水量**，可分离"增益 vs 延迟"的辨识歧义，对 A1/A6 有直接增益。

---

## 4. 结果登记表（跑完只填此表，不改上文）

| ID | H18 | Δ vs 0.4840 | 分箱极差 | frac_neg | 判定 | 备注 |
|---|---|---|---|---|---|---|
| **A1** | **0.5430** | **+12.2%** | **1.85×（+64%）** | 待测 | **REJECT** | α_τ=0/α_UA=0.8 冻结；bins .366/.427/.599/.678/.645 |
| A2 | **0.778** | **+60.7%** | **2.25×（+99%）** | 1.000/1.000 | **REJECT** | initial_state_mode='steady'；bins .434/.651/.887/.940/.977 |
| A3 | | | | | 待排 | |
| A4 | **0.447** | **−7.6%** | **1.48×（+31%）** | 1.000/1.000 | **INCONCLUSIVE** | k 按负荷调度（w=−0.119，因子 ±3.5%）；bins .385/.391/.571/.449/.441 |
| A5 | **挂起** | — | — | — | **HOLD** | 真实水煤比测点入模型需边界通道扩展（v0.6 Phase 2 对侧 src），探针侧仅可覆写 `_substep` 无法承载新通道；A5-lite 代理方案用户不选，等通道落地跑真测点版 |
| A6 | **0.490** | **+1.2%** | **1.22×（+8%）** | 1.000/1.000 | **INCONCLUSIVE** | 压力第二调度（a_τ=0.856, b_τ=0.876）；bins .506/.467/.541/.492/.443；Q3 靶点未达（+7.6%） |
| A46 | **0.618** | **+27.7%** | **1.51×（+34%）** | 1.000/1.000 | **REJECT** | A4+A6 联合（w 反号 +0.079；a_τ=0.942, b_τ=0.782）；bins .596/.687/.723/.608/.479 |
| A9 | **0.593** | **+22.5%** | **1.68×（+49%）** | 待测 | **REJECT** | 只调度 τ_mix、不调度 τ_B；bins .439/.554/.736/.733/.501 |

### A1 判决说明（否决我自己的机制假设）

**假设（事前）**：负荷相关慢动态来自金属热惯性 Cm/UA，学到的 α_τ=1.351 只是它的**代理**；
故用物理正确的 UA∝ṁ^0.8 替代、并撤掉 τ 调度（α_τ=0），应当至少不差于自由 τ 臂。

**实测：REJECT（+12.2%）**。若 α_τ 真的只是代理，替换成物理正确的通道不该变差 12%。

**分箱结构是关键证据**（UA 因子 Q1 0.656 / Q5 1.486）：

| 箱 | 基线 | A1 | 变化 |
|---|---|---|---|
| Q1 | 0.459 | **0.366** | **−20%** |
| Q2 | 0.455 | 0.427 | −6% |
| Q3 | 0.503 | 0.599 | +19% |
| Q4 | 0.513 | **0.678** | **+32%** |
| Q5 | 0.489 | **0.645** | **+32%** |

→ **UA 调度在低负荷有效、在高负荷有害**。低负荷 UA 被调小（0.656）→ 慢动态变长 → 改善；
高负荷 UA 被调大（1.486）→ 时间常数缩短 → 恶化 32%。

**结论（三条，写入发现记录）**：
1. **数值比值吻合 ≠ 机制辨识**。实测 2.33 / Dittus-Boelter 2.26 / 文献 2.2 三方吻合，
   只说明"存在一个 ~2.3 的时间常数比"，**不能确定该比值由哪个参数承载**——τ_mix 调度、
   UA 调度、Cm 调度都能产生同一比值。这正是本项目反复遇到的弱可辨识在诊断层面的表现。
   **我此前把这个巧合读成了机制证据，属于过度解读。**
2. **模型的账上，负荷依赖挂在喷水/输运通道而非金属通道**：撤掉 α_τ 后即使给了物理正确的
   UA 调度也补不回来（+12.2%）。
3. **调度形式应是非单调/分段的**：A1 低负荷 −20%、高负荷 +32%，说明纯幂律在全量程上
   错向；这与 Fan2021 用**多项式**（非单调）而非幂律、以及 A6（压力在 Q3 解耦）互相印证。

### A9 判决说明（s5 τ_B 混淆修正——方向与预期相反）

**假设（事前，s5 偏离记录）**：`_substep` 把制粉滞后 τ_B 与蒸汽输运 τ_mix1/2
一起乘流量因子，是设计缺陷（τ_B 属磨煤机动态，Fan2021 中与蒸汽侧解耦）。A9 只调度
τ_mix、不调度 τ_B，预期**至少不差于**自由 τ 臂（若明显更好 → 证实混淆代价）。

**实测：REJECT（0.593，vs 自由臂 +27.1%、vs 基线 +22.5%）**。拿掉 τ_B 调度反而
恶化，且集中在中高负荷：

| 箱 | 基线 | 自由 τ | A9 | A9−自由 |
|---|---|---|---|---|
| Q1 | 0.459 | — | 0.439 | — |
| Q3 | 0.503 | 0.568 | **0.736** | +0.17 |
| Q4 | 0.513 | — | **0.733** | — |
| Q5 | 0.489 | — | 0.501 | — |

val 也同步恶化（1.0956@86 → 1.229@51），α_τ 学成 1.163（自由臂 1.351）。

**结论（两条）**：
1. **模型的 τ_B 不是物理制粉滞后**：基线自学 τ_B=409-493s（先验 120s，4×），
   τ_mix 700s（先验 80s，9×）——两个参数早已偏离物理标签，成为**可互换的慢滞后
   自由度池**。模型账上的负荷依赖滞后分布在这个池上，拿掉 τ_B 的调度自由度，
   Q3/Q4 直接爆炸。与 A1 同构：**物理上正确的结构约束（制粉与蒸汽解耦）在弱可辨识
   下不自动带来收益**。
2. **调度战役五臂全部不晋级**（自由 τ −3.6% / 钉先验 +0.8% / UA +12.2% / A9
   +22.5% / UA^0.8 变体已 REJ），且两个"更物理"的变体（A1/A9）都**显著更差**——
   "调度不是瓶颈"的结论进一步加强，参数调度系的设计空间价值应如实降权。

### A2 判决说明（纯物理初值消融——O1 老判据在 v2.1+120/20 下翻转）

**假设（事前）**：观测器的学习式初值修正对 H18 有实质贡献；反向证据是 O1 老判决
（learned MIXED / hybrid REJECTED，v0.2 预算 + 旧错侧记录），需在 120/20 + v2.1
下复测。本条是诚实性检查，不预设方向。

**实测：REJECT（0.778，+60.7%；val 1.376@98 vs hybrid 1.1096@86，+24%）**。
纯物理稳态初值比 hybrid 显著更差，且恶化结构高度可读：

| 箱 | hybrid 基线 | A2 steady | 变化 |
|---|---|---|---|
| Q1 | 0.459 | 0.434 | −5%（持平略好） |
| Q2 | 0.455 | 0.651 | +43% |
| Q3 | 0.503 | 0.887 | +76% |
| Q4 | 0.513 | **0.940** | **+83%** |
| Q5 | 0.489 | **0.977** | **+100%** |

**结论（三条，写入发现记录）**：
1. **NN 初值修正买的是 ~60% 的 H18，且它是负荷依赖误差结构的主要压平器**：
   hybrid 分箱极差 1.13×（几乎全平）→ steady 2.25×，误差随负荷单调放大——物理稳态
   解与真实初态的偏离在高负荷最大（负荷越高、动态越远离稳态），学习式修正正是把
   这个失配吸收掉了。低负荷 Q1 steady 反而略好（−5%），与"稳态近似在低负荷更准"
   的物理直觉一致。
2. **O1 老判决（learned/hybrid 无增益）在 v2.1+120/20 下翻转**：hybrid 初值不是
   可消融的装饰，是精度支柱。这回应了可信度审计对 O1 continuity 证据的关切——初值
   修正机制确有实质作用，其正式证据须在 v0.7 重发中按 paired-NLL + state continuity
   合同重建。
3. **方向不受初值模式影响**（v1/v2 frac_negative 均 1.000）——方向持证来自
   transition 动力学而非初值路径，与"再湿项反号"结论正交。

### A4 判决说明（k 负荷调度——战役首个过 −5% 线，但把负荷均匀性还回去了）

**假设（事前）**：燃料→金属吸热增益 k 随负荷变化（低负荷燃烧恶化/炉温下降），
Fan2021 用负荷多项式调度 k₁；事前证据=单一常数水煤比下的能量失配分箱均值
−43.1（Q1）→ +107.2（Q5）t/h 单调摆动。

**实测：INCONCLUSIVE（0.447，Δ −7.6%；val 1.123@86 vs 基线 1.1096@86）**。
这是调度战役**第一个越过 −5% 精度线的臂**（0.447 < 自由 τ 0.4666 < 基线 0.4840），
方向双阀 1.000 全过。但按预注册 PROMOTE 三条件并取：分箱极差 1.13×→1.48×（+31%）
超过 +10% 上限 → **不晋级**。

| 箱 | 基线 | A4 | 变化 |
|---|---|---|---|
| Q1 | 0.459 | **0.385** | **−16%** |
| Q2 | 0.455 | 0.391 | −14% |
| Q3 | 0.503 | **0.571** | **+13.5%** |
| Q4 | 0.513 | 0.449 | −12% |
| Q5 | 0.489 | 0.441 | −10% |

**结论（三条）**：
1. **w 只学到 −0.119（k 因子摆动 ±3.5%）却买了 −7.6% 总体精度**——收益来自低/高
   负荷两端（Q1 −16%、Q4/Q5 −10~12%），恰是事前能量失配最大的区域；k 在 s<1 升
   （低负荷补燃）、s>1 降（高负荷减过热输入），方向与物理直觉一致。
2. **Q3（s≈1）净恶化 +13.5%**：s≈1 处调度因子≈1（几乎不动），Q3 的恶化不是调度
   直接作用，而是模型其他参数为利用新自由度重新分配（基线 k 是全域折中，有了
   调度后折中解除，中间负荷反而失去旧折中的保护）——与 A9 同一机制（参数池
   再分配），代价落点不同。
3. **战役结论微调**：六臂中 A4 是唯一精度过线臂，但以接线修正买下的负荷均匀性
   为代价（1.13×→1.48×）。"调度不是瓶颈"的精确表述升级为：**调度系偶有精度
   收益（A4 −7.6%），但无一臂能在不牺牲负荷均匀性的前提下晋级**；单种子探索性，
   不升路线判决。

### A6 判决说明（压力第二调度——Q3 靶点未达，val/H18 再度错位）

**假设（事前）**：纯流量调度在压力与流量解耦的工况下误配；事前偏相关
+0.954/+0.956（控流量后压力对停留时间代理）、Q3 箱内压力散布 12.9-22.5 MPa、
自由 τ 臂唯一恶化箱=Q3。**判定补充（预注册）：本臂额外要求 Q3 不恶化，否则
即使总体 −5% 也记 INCONCLUSIVE。**

**实测：INCONCLUSIVE（0.490，Δ +1.2%，落入 −5%~+5% 带；val 1.083@86 =
战役最优，−2.4% vs 基线）**。方向双阀 1.000 全过。

| 箱 | 基线 | A6 | 变化 |
|---|---|---|---|
| Q1 | 0.459 | **0.506** | **+10%** |
| Q2 | 0.455 | 0.467 | +3% |
| Q3 | 0.503 | **0.541** | **+7.6%（靶点未达）** |
| Q4 | 0.513 | 0.492 | −4% |
| Q5 | 0.489 | **0.443** | **−9.4%** |

**结论（三条）**：
1. **压力维度被实打实用起来了**：b_τ 学到 0.876（有界带 0-1.5 的中上段），而
   流量指数从自由臂的 1.351 **腰斩到 0.856**——模型把滞后的负荷定位在流量和
   压力两个变量之间重新分配，总调度量守恒（0.856+0.876 ≈ 1.35+0.38 同量级）。
   但分配的结果是**误差在箱间搬家**（Q1 +10% ← → Q5 −9.4%），总体 −1.2% 打平，
   **Q3 这个设计靶点没有改善反而 +7.6%**——压力-流量解耦假说未被数据买单。
2. **val/H18 错位第 7 次**：val 战役最优（1.083）但 H18 比基线差（0.490 vs
   0.484）。按 val 选会选中 A6，按 probe 选会留在基线——这正是对侧 Task 2
   （paired ΔNLL 判决）要治的病，本臂是又一个实例证据。
3. **对联合臂的含义**：A6 单独无收益、A4 单独有收益但 Q3 代价。A4+A6 联合的
   价值在于：A4 的 k 调度（燃料侧）与 A6 的压力 τ 调度（输运侧）作用在不同
   物理通道上，理论上互补——这是下一步的预注册理由。

### A46 判决说明（A4+A6 联合——比任何单臂都差，调度耦合即毒药）

**假设（事前）**：A4（k 燃料侧）与 A6（流压 τ 输运侧）作用在不同物理通道，
理论上互补，联合臂应至少不差于两单臂。

**实测：REJECT（0.618，+27.7%；val 1.264@52 vs 基线 1.1096@86）**。
联合臂**比 A4（0.447）和 A6（0.490）都显著更差**，也比基线差 27.7%。
方向双阀 1.000 仍全过。

| 箱 | 基线 | A4 | A6 | A46 |
|---|---|---|---|---|
| Q1 | 0.459 | 0.385 | 0.506 | **0.596** |
| Q2 | 0.455 | 0.391 | 0.467 | **0.687** |
| Q3 | 0.503 | 0.571 | 0.541 | **0.723** |
| Q4 | 0.513 | 0.449 | 0.492 | **0.608** |
| Q5 | 0.489 | 0.441 | 0.443 | 0.479 |

**结论（三条）**：
1. **w 直接反号**：+0.079（A4 单独 −0.119）。四个调度旋钮（a_τ/b_τ/a_UA/w）
   同时可学时互相打架，k 调度的方向都被带反——这不是"两个正确叠加"，是
   **弱可辨识在调度维度的直接观测**：每加一个旋钮，优化就多一条把负荷依赖
   分配错的路。best@52 早停也说明盆地在恶化。
2. **中负荷整段爆炸**（Q2 +51%、Q3 +44%），Q5 微降——误差从两端被赶进中间。
   与 A9 同构（参数池再分配），但这次发生在**调度旋钮之间**而非参数之间。
3. **对"四合一"的预判（必须诚实写）**：A46 已经证明**两个调度的联合就 REJECT**，
   "A4+A5+风煤比+A6 四合一"按此轨迹大概率更差——弱可辨识下调度自由度增加
   单调恶化（自由τ −3.6% → A4 −7.6% 是唯一例外）。若仍要试四合一，预期必须
   写成"大概率 REJECT，其价值是关闭路线而非找收益"；且应先验证**单臂间
   w/a_τ 的符号冲突**（A46 的 w 反号）是否能在联合中修复（如钉死单臂学到的
   符号再联合）。

### 调度战役汇总（八臂，同协议 seed0，修正 v2.1 记录，网格物性）

| 臂 | H18 | Δ | 分箱极差 | val_nll | α_τ | α_UA | 判定 |
|---|---|---|---|---|---|---|---|
| 基线无调度 | **0.4840** | — | 1.13× | 1.1096@86 | — | — | — |
| LPV 自由 τ | **0.4666** | −3.6% | 1.37× | **1.0956@86** | 1.351 | 0.046 | INCONCLUSIVE |
| LPV 钉先验 τ | 0.4880 | +0.8% | 1.70× | 1.319@103 | 2.418(cap) | 0.050 | INCONCLUSIVE |
| A1 纯物理 UA | 0.5430 | **+12.2%** | 1.85× | 1.162@84 | 0（冻结） | 0.8（冻结） | **REJECT** |
| A9 只调度 τ_mix | 0.593 | **+22.5%** | 1.68× | 1.229@51 | 1.163 | 0.045 | **REJECT** |
| **A4 k 负荷调度** | **0.447** | **−7.6%** | 1.48× | 1.123@86 | w=−0.119 | 0.045 | INCONCLUSIVE |
| A6 压力第二调度 | 0.490 | +1.2% | 1.22× | **1.083@86** | 0.856 | 0.047 | INCONCLUSIVE |
| A46 A4+A6 联合 | 0.618 | **+27.7%** | 1.51× | 1.264@52 | 0.942 | 0.048 | **REJECT** |

**战役级结论**：**八个物理动机的调度变体无一晋级**。唯一越过 −5% 精度线的是
A4（k 调度，−7.6%），但以负荷均匀性为代价（1.13×→1.48×，Q3 +13.5%）未过
PROMOTE 三条件；A6（压力 τ 调度）val 战役最优（1.083）但 H18 +1.2%、Q3 靶点
未达；**A46 联合比两单臂都差（+27.7%），w 反号**——调度自由度增加在弱可辨识下
单调恶化，联合路线基本关闭。对比接线修正的 **−33%**（且分箱变平 1.13×），
**参数调度不是本问题的瓶颈**——这个结论应当
如实写进论文，而不是把 −3.6% 包装成收益。

### 附：LPV 调度两臂（A1 的前置对照，本预注册立档前已启动，按同判据裁定）

| 臂 | H18 | Δ | 分箱极差 | val_nll | α_τ | α_UA | 判定 |
|---|---|---|---|---|---|---|---|
| LPV 自由 τ | 0.4666 | **−3.6%** | 1.37×（+21%） | **1.0956@86** | 1.351 | 0.046 | **INCONCLUSIVE**（未达 −5%，且极差超 +10% 上限） |
| LPV 钉先验 τ | 0.4880 | +0.8% | 1.70×（+50%） | 1.319@103 | **2.418（撞 cap 2.5）** | 0.050 | **INCONCLUSIVE**（副指标全面变差） |

**学到的慢通道参数（三臂对照，先验值括注）**：

| 臂 | τ_mix1 (80) | τ_B (120) | UA0 (600) | Cm0 (60k) | Cm/UA |
|---|---|---|---|---|---|
| 基线无调度 | **700s** | 409s | **125** | **236k** | 1890s |
| LPV 自由 τ | 667s | 493s | 129 | 253k | 1962s |
| LPV 钉先验 | 80（钉死） | 120（钉死） | 104 | 236k | 2269s |

**结论（写入 §5 偏离/发现记录）**：
1. **"双重补偿"假设被否决**。钉死 τ=80s 后 α_τ **饱和到 2.418（cap 2.5）** 仍只能把
   τ_eff 推到 286s（Q1），远低于基线自学的 700s，且 val 恶化 +19%、多训 17 轮。
   → **700s 的长滞后是真实需要的，物理先验 80s 低估近 9 倍，且该差距不能由负荷调度解释**。
2. **三个慢参数在共同代偿同一个缺失动态**：τ_mix 700s（先验 80，×8.8）、
   UA 125 kW/K（先验 600，÷4.8）、Cm 236k（先验 60k，×3.9），Cm/UA ≈ 1890-2270s。
   这是弱可辨识的物理根源，也支持"τ_mix 实际承担金属热惯性通道"的解释。
3. **α_UA 第四次不被采纳**（0.016 / 0.046 / 0.046 / 0.050，四次独立训练一致），
   尽管实测延迟比 2.33 与 Dittus-Boelter 预测 2.26 几乎相等。此矛盾由 A1 判决。
4. **Q3 恶化复现**（0.503→0.568，自由臂），与压力-流量解耦分析预测的位置一致，
   支持 A6（压力作第二调度变量）。

## 5. 偏离记录

（若实际执行偏离本预注册，在此登记原因与时间）

- 2026-08-26 立档。
- 2026-08-26 A5 由"条件化"升级为"独立臂"：原判断基于我自拼的 `主蒸汽流量/未校正总煤量`
  代理（a=4.485，错值），改用 DCS 真实水煤比测点（371 列，tag 均值 3.662）后，负荷二次
  拟合仅解释 R²=0.525，故 A4/A5 不冗余。
- 2026-08-26 LPV 两臂在本预注册立档前已启动，事后按同一判据裁定并登记于 §4 附表；
  其"双重补偿"设计假设经 `--pin-prior` 臂**否决**。
- 2026-08-26 **A1 启动前 check 发现调度实现的一处设计缺陷**（用户要求复核时查出）：
  `_substep` 中 `tau_b = (tau_b * inv).clamp(min=floor)` —— **制粉系统滞后 τ_B 也被蒸汽
  流量调度了**。τ_B 是磨煤机动态（取决于磨负荷/存煤量），与蒸汽输运不是同一物理量；
  Fan 2021 中制粉是独立方程 `dx₁/dt = −x₁/c₀ + e^(−τs)u₁/c₀`，与蒸汽侧解耦。
  - **对 A1 无影响**（α_τ=0 → inv=1.0，τ_B 不动，已逐位验证）；
  - **对已完成的自由 τ 臂有影响**：α_τ=1.351 使 τ_B 在 Q1 放大 2.04×、Q5 缩至 0.51×，
    把磨煤机动态与蒸汽输运混为一谈；这可能是该臂增益仅 −3.6%、分箱极差反而 +21% 的
    部分原因。
  - **新增补充臂 A9**（排在 A3 之后）：只调度 τ_mix1/τ_mix2，**不调度 τ_B**，其余同自由
    τ 臂。若 A9 明显优于自由 τ 臂 → 证实混淆代价；若打平 → τ_B 调度无害，自由臂结论不变。
  - 已完成臂的数字与判定**不因此改写**（预注册纪律），只作解释性说明。
- 2026-08-28 **A5 挂起（HOLD）**：真实水煤比测点（371 列）作为模型输入需要边界通道扩展
  （boundary_ext 模型侧消费），属 v0.6 Phase 2 对侧 src 工作（`_unpack_boundary` 硬校验
  7 列、`step()` 固定解包）；探针侧注入纪律（仅覆写 `_substep`）无法承载新通道。
  A5-lite（用已有边界通道 D/u_b 拼代理、机制先行）已向用户提出，用户选择不执行，
  等通道落地后跑真测点版。A4 于同日启动，为调度战役第六臂。
- 2026-08-28 A9 由"偏离记录"升级为已执行臂并 REJECT（+22.5%），详见 §4 结果表与判决说明。

### A1 启动前 check 清单（2026-08-26，逐项实测通过）

| 项 | 实测 | 期望 |
|---|---|---|
| α_τ | 0.0000025（τ 因子 1.0000013） | 0 |
| α_UA | 0.800000（raw 0.6931） | 0.8 |
| 冻结 | ep22 时 raw 仍为初值，未漂 | requires_grad=False 生效 |
| UA 因子 | Q1 0.656 / Q5 1.486 | 随流量递增 |
| Cm/UA 比 Q1/Q5 | **2.264** | 实测 2.33 / DB 2.26 |
| 物性 | GridThermoProperties（ledger 回读） | Grid |
| 记录 | canonical_sideA_v1fixed（v2.1） | corrected |
| τ 基值 | 211.6s（ep22，自由可学） | 不钉死 |
| s 范围 | 0.52-1.83，clamp(0.1,3.0) 不触发 | 不饱和 |
| 稳定性 | 金属方程 UA 在分母（半隐式），UA 增大更稳 | 不失稳 |
