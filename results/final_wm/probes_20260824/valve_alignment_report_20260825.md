# 阀位/温度侧对齐验证报告（vs 377 列最终数据文件）

2026-08-25, data-side forensics, execution-side.

## 结论表

| 问题 | 结论 | 证据 |
|---|---|---|
| mainT 取数是否混了阀位 | **没有** | A 文件一级/二级阀位 = 377 一级/二级 A 阀位反馈 (+1.000)；B 文件同理 (+1.000) |
| mainT A/B 文件的温度是哪个物理侧 | **A=左侧, B=右侧** | 与 377 显式左/右列 +1.000（末级/一过/二过出口全对） |
| 二级阀物理接线 | **交叉控制：A 阀→右侧, B 阀→左侧** | 零滞后 -0.614/-0.323 + 30s 滞后差分同向；与用户"a侧二级减对右侧"一致 |
| 一级阀物理接线 | **存疑** | 零滞后两阀均更像右（-0.686/-0.615，受右侧方差大 1.7x 污染）；30s 滞后差分一级A→左侧更强（-0.224 vs -0.123）；一级A/B 反馈相关 +0.784=联动控制 |
| canonical 桥接（A记录取B阀, B记录取A阀） | **二级正确；一级取决于现场真值** | 见上两行 |
| sh1_inlet 映射 | 正常（HIGH/CLOSED） | mapping_audit.json L76；"缺失"为历史注记 |

## 影响评估

1. 二级阀：现 canonical 记录**接线正确**，无需修正 → sh1/sh2 大误差的解释不在阀位侧。
2. 一级阀：若现场一级同为交叉（用户口径），现记录正确；若一级为同侧接线，
   桥接的 v1 错侧——但一级A/B 联动（+0.784）使 v1 两候选信号近似可互换，影响有限。
3. 误差归因按优先级：缺输入（磨/烟气, v0.6）> rewet 消融 > 一级联动结构未建模。

## 377 列最终文件的映射建议（供 v0.6 管线）

- obs（5 温度）: 左侧列（选择后左侧一过入口/出口, 左侧二过出口, 二级减温器左侧入口, 选择后左侧末级过热器出口汽温）
- action v1/v2: 用**阀位反馈**列（与 canonical 身份一致，勿用主调输出——那是 PID 指令）
  - obs=左侧 → v2 = 过热器二级减温器B侧喷水调节门阀位反馈（交叉控制实测）
  - v1 侧别待现场确认（见上）
- 数据源: all_merged_10s.csv（138 天超集，含 canonical 82.6 天窗）

## 待用户确认

现场一级减温喷水：A 侧一级阀喷的是同侧还是对侧？
