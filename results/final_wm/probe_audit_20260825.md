# 执行侧探针批次审计（eec04f9，2026-08-25）

对象：`results/final_wm/probes_20260824/`（5 脚本 + 2 JSON）+ `probe_notes_20260824.md` + v05 一行热修。
性质：exploratory 探针，非矩阵协议，不产生判决；本审计核方法、核数字、定口径。

## 1. 过程合规（一项偏差，已追认）

- **分工偏差**：探针脚本由执行侧新写（约定为"Linux 只做执行回传，代码由设计侧出"）。
  鉴于产出为只读探针、未触冻结路径、方法经本审计抽查合格，**追认为 exploratory
  证据**，后续探针需求仍由设计侧发单。
- **v05 热修**（`evaluate()` 末尾补 `model.train()`）：+1 行，仅影响训练期 val
  循环后的模式恢复，不改 eval 语义；本地已完成运行不受影响（Windows 侧未触发
  cuDNN 崩溃）。审过接受。
- **跨平台差**：执行侧重评 LSTM s0 H18=0.509 vs 本地 0.496（±0.01-0.02，
  cudnn 内核差异），无判决影响。

## 2. 数字核验（全部一致）

- `phys_per_segment.json`：norew 主汽温 H18 分箱 0.64/0.69/0.93/0.89/0.68°C
  （Q1-Q5，各箱 50-52 窗），sh1_inlet 5.90/4.58/4.87/3.76/4.30（低负荷最差）、
  sh1_outlet 3.98→1.99 单调随负荷改善——与笔记表格逐项一致。
- `bb_per_segment.json`：iTransformer 0.34-0.38 总体、LSTM 0.46-0.57，
  与 v05 全量包（本地产物）交叉一致。
- **结论修订**：黑箱在**全部五个负荷箱**领先物理臂 1.4-2.3×（低负荷最大），
  非仅总体中位差异。TODO 行 62 "平价档"口径过宽，已修订为"精度全线落后
  ~1.7-2×，响应方向独占持证"。公平性不对称两条（InstanceNorm=persistence
  捷径；单通道专用 vs 5 通道联合损失）成立且值得公平化探针，但**不改变
  当前可声称口径**：同 oracle 信息集下主汽温精度黑箱更优。

## 3. 数据资产发现（重大，升级 v0.6）

`all_merged_10s.csv`（3.8GB / 381 列 / 139 天，覆盖 canonical 82.6 天）含
磨×8（炉烟温度/流量/电度表）、给煤×8、烟气氧量/烟温、8 角配风、管壁温、
吹灰程控、分级减温阀位——**canonical 41 列之外的全部未入模**。
含义：sh1_inlet "信息缺失"病因从假说升级为**数据侧可验证**（磨组合/烟气
硬信号在手）；v0.6 通道扩充从"需现场补点"降级为"纯管道工程"。
sh1_outlet 病因（低负荷近饱和段润湿被 norew 整体移除→gate 分段启用）属
模型侧机制修复，与 v0.6 互补不冲突。

## 4. 闭环地形（信任边界证据，论文可用）

- 恒定工况 PI（v2 控 571°C，120min）：多数负荷收敛 ±3°C，分箱稳态偏置
  +6.2/+1.1/−3.6/+3.4/+1.1°C；v2 喷水需求 W 形地形与数据包络一致。
- 快变斜坡（7.4 kg/s/min 无燃料前馈）：失控平台 635/528°C；喷水前馈无效。
  **根因判读接受：closure 在 (state,boundary) 联合支持域外外插，而
  counterfactual 支持域纪律只查 action 域**——信任边界节直接引用。
- 开环合成工况漂至 670°C 撞 h_hi=3700 clamp → 低负荷增益伪零为**数值伪影**，
  审计留痕防误读。

## 5. 待决（需用户裁定）

1. 磨切换事件研究（磨组合变更 vs sh1_inlet 跳变，纯数据侧）是否立项；
2. 论文主黑箱基线口径：v05 现状（InstanceNorm+单通道）vs 公平化版本
   （去 InstanceNorm / 多通道联合），或两者并报；
3. 探针脚本入库位置：现处 `results/final_wm/probes_20260824/`，建议移
   `experiments/final_wm/probes/`（代码）+ 保留结果 JSON 于 results。
