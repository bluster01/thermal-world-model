# FMTS norew 配对证据：回传独立核对与论文边界

2026-09-08。回传提交 a34481f；执行脚本 db9447b。批次状态：RETURN_AUDITED_NO_FULL_R1_UPGRADE。当前授权的三臂、三种子预测/响应补评已完成，无需为本批追加运行。

## 已闭合的证据

- 执行脚本 SHA-256 与冻结提交逐字节一致；返回日志 COMPLETE，stderr 空。
- 九份 H18 预测张量的 MAE、NLL、CRPS、day_ids 均在本地与原 committed metrics 逐元素完全相同。
- 从原 canonical train/validation 前缀重建各预测与响应窗口索引，与回传一致；完整索引对齐不只依赖 day_ids。
- 从原动作历史独立计算 36 组干预与基线支持域掩码，逐元素一致；跨臂同 seed/阀/视界的索引与掩码相同。
- 六组配对 MAE 等日 bootstrap CI 在本地复算一致（仅允许 1e-6 的算术归约差异，不改方向判据）。
- norew 的 12 组响应均值及 CI 与原 R1 报告差异严格为零，支持域掩码也一致。
- 未调用模型推理或训练；本次核对只处理保存结果和 train/validation 数据，test 数值解码 0 行。执行侧九次参数不变断言均通过。

响应的逐窗口温度差未由原冻结脚本保存，故本地没有独立重新 bootstrap 响应 CI；这些 CI 是哈希核对后的冻结函数执行输出。原 norew CI 可与既存 R1 交叉核验。此限制不应被描述为整个配对回传缺失。

## 核心结果

预测指标：Side A，五温度 H18，256 窗口逐步/逐通道 MAE，再对三 seeds 等权平均。响应指标：每 seed 双阀 H18/H60，末十步末过温度差，等 UTC 日 bootstrap CI。以下响应计数包括原样保留的越支持域诊断，不能称为正式 R1 通过率。

| 配置 | 预测 MAE（°C） | 响应 CI 全负 | 响应 CI 全正 | 响应 CI 跨零 | 存在越支持域的单元 |
|---|---:|---:|---:|---:|---:|
| physics_only | 1.719237 | 1/12 | 7/12 | 4/12 | 11/12 |
| closure_cons | 1.602662 | 9/12 | 0/12 | 3/12 | 11/12 |
| closure_cons_norew | 1.886029 | 12/12 | 0/12 | 0/12 | 11/12 |

用户此前解释“选择 norew 是为了解决方向问题”，现在得到同配置、同输入、同干预的配对支持：physics_only 多个单元出现正响应；closure_cons 大幅改善，但还有三组 CI 跨零；norew 十二组 CI 均在负侧。closure_cons 的跨零结果是证据不确定，不应统称为确定反向。

关闭再润湿：closure_cons → closure_cons_norew 的三种子均值预测差 **+0.283368°C（+17.681%）**，各 seed 均恶化。

| seed | 等日配对 MAE 差（°C） | 95% 日块 CI（°C） |
|---|---:|---:|
| 0 | +0.305572 | [+0.186556, +0.432114] |
| 1 | +0.374551 | [+0.234508, +0.530746] |
| 2 | +0.202579 | [+0.079438, +0.357995] |

等日 CI 的点估计与上面的等窗口均值差采用不同权重，不将其当成同一个估计量混写。三 seed 区间均为正，但不制造跨 seed 的总体显著性。

原再润湿结构内，physics_only → closure_cons 的预测改善为 0.116576°C（约 6.78%）；三 seed 等日配对差 CI 均负。这只能归因于该结构下加入 closure 并重新拟合的配置对比，不能作为 norew 路径内“追回精度”的证据。

## 当前图 2 可写到哪里

可写：在冻结的动作探针下，关闭再润湿使十二个阀/视界/种子响应单元的 CI 全部落在冷却方向，同时五温度 H18 MAE 增加 0.283°C（17.7%）。同时明确 11/12 单元存在越支持域步骤，结论属于保留外推标记的方向诊断。

不可写：为了获得“已验证现场响应保真度”付出 17.7% 代价。方向正确不提供现场幅值/时序真值，支持域问题也未解除。不得把“全部 CI 负”标为 R1 PASS；原 norew R1 仍 INCOMPLETE，本批没有运行 leakage 辅助训练，其他两臂亦不宣告完整 R1。

图 2 的第二个问题仍待匹配对照：需要“无 closure + norew + 同 hybrid 初态”的保存 checkpoint，对比 closure_cons_norew。当前原包没有该臂；先由执行侧核实是否存在其他匹配产物。若没有，必须另议新增训练，当前不训练，也不通过即时关掉已训练 closure 冒充独立训练对照。

不等待 Side B、不重开 Windows 内核定位、不改变支持域阈值、不筛样或补抽样来寻求通过。现场保真度增强证据仍属于可选的后续科学问题。本次没有自动改图、改论文或启动新实验。
