# FIXC 设计稿：残差动态化（模型侧慢动态，修复 C）

日期: 2026-08-17 | 状态: 预注册冻结

## 1. 问题定位（审计+身份诊断后的收敛结论）

干态闭环不稳定、湿态闭环 G1 形态未过，四环节排除三（PI 真实参数不可动 / 执行机构=快伺服+0.14%/s 实测约束 / W 联动数据回归），唯一未愈环节 = **模型对象动态过快**（Step⑤ 病灶②，τ63=10s→滞后后 ~100-200s vs 真实 ~600s）。根因 = F2 发现：**瞬时残差抵消物理热惯性**。

## 2. 修复：残差注热通道加一阶滞后（可学习）

- 结构: z_lag += dt/τ_res·(z − z_lag)，τ_res = 120s×softplus(raw_res)（先验 120s，与残差、τ_sw/τ_sens 联合训练）
- 位置: 残差输出 z 注入 Tm/h 方程**之前**；z_lag 为新增状态，训练/评估同路径
- 意义: 残差不能再逐步即时修正 → 物理惯性得以保留 → 对象响应变慢

## 3. 预注册判定（冻结）

- **C1**: 干态闭环 (真实 POU107 PI + 阀位速率限制 0.0137/步, 无 τ_a 块) norm600∈[0.8,1.2] ∧ tail_std≤0.05
- **C2**: 开环 v2 耦合阶跃 τ63∈[240,900]s 且 θ∈[0,300]s（湿+干，原 Step⑤ G2 门槛首次真正复测）
- **C3**: 湿态闭环 G1 原门槛: 各锚点 |Δ|≤0.15 且 600s 锚点 ≤0.10
- **C4**: rollout 2seeds mean ≤ 3.5（慢化精度代价预算）
- **C5** (审计): τ_res 学得值报告——数据是否支持"残差应该慢"（τ_res 显著 >0 且未钉边界）

裁决: C1∧C2∧C3∧C4 = PASS。任一 FAIL → 报告归因。

## 4. 产物

out/fixc_slowdyn_summary.json, out/figs/fig24_slowdyn.png, model_res_qslow_seed{s}.pt
