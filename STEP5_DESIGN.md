# Step⑤ 设计稿：动态响应域验证（阶跃响应 → 传递函数 → 控制嵌入跳板）

日期: 2026-08-17 | 状态: 草案待用户确认 | 仓库: ~/.hermes/workspace/adhoc2_lumped_enthalpy

## 1. 目标

在"动态响应域"验证 qh（灰盒+蒸汽侧焓残差）模型的可信度，而非继续用点wise RMSE。通过 = 模型可用于传递函数提取与控制嵌入设计（路线 A/B）。不通过 = 先修段0两相结构再谈控制。

## 2. 已知前提（本设计稿必须继承）

- **对齐协议**：评估真值一律 truths = T[start+1:]（修复 10e 发现的 rollout 1 步错位；聚合影响 ≤0.03°C 但 10s 相移会污染纯延迟辨识）
- **已知病灶**：qh 在 sh1_out 首步误差 21-25°C（喷水混合结构缺陷），rollout 稳定在 5-6；v2 首步 3.4 但 rollout 爆炸到 21.4。G4 记录不设门槛。
- 金标准：exp_099 实测事件研究曲线 — main 温度对 SP 阶跃的归一化响应（60s 0.000 / 120s 0.10 / 180s 0.17 / 300s 0.49 / 420s 0.70 / 600s 0.97）

## 3. 注入协议

- 模型 qh（seed0），两个工况点：湿态代表窗（pm ≤ 22.064）与干态代表窗（pm > 22.064）各 1 个，窗口起点状态取自真实数据
- 时域 6000s（600 步），外生除注入通道外恒定于窗口起点值
- 通道：
  - A：二级减温阀位 v2 +5%（保持 v1 不变）
  - B：一级减温阀位 v1 +5%
  - C：闭环 SP 阶跃——用 POU107 解析出的二减 A 侧 PI 参数（Kp/Ti 调度表；若无调度表取中位值并在局限注明）在模型外闭环：SP +2°C → PI → v2 → 模型 → main 反馈
- 输出：5 个温度通道的阶跃响应轨迹 + 归一化响应曲线

## 4. FOPDT 提取

每通道每工况：增益 K = ΔT_ss/Δu（符号必须物理正确：阀开→温降），τ_63 = 达到 63% 稳态的时间，θ = 纯延迟（响应起始滞后）。同时输出 5 个锚点（60/120/180/300/420/600s）的归一化响应值，与 exp_099 曲线直接比对。

## 5. 预注册判定（冻结，不可改）

- **G1（主门）**：闭环 SP 阶跃 → main 的归一化响应与 exp_099 锚点曲线比对，各锚点 |Δ| ≤ 0.15，且 600s 锚点 |Δ| ≤ 0.10
- **G2**：开环阀位阶跃 → main 响应单调、方向正确（阀开→温降）、τ_63 ∈ [240, 900]s、θ ∈ [0, 300]s（物理合理区间）
- **G3**：湿/干两工况点开环增益同号且比值 ∈ [0.3, 3]
- **G4（审计，不设门槛）**：阀门→sh1_out 通道首步偏差记录，作为段0结构的量化病灶证据

裁决：G1∧G2∧G3 = PASS（模型进入 TF 提取与嵌入设计）；任一 FAIL = 不通过，转段0两相结构修正。

## 6. 产物

- out/step5_summary.json（判定 + 各通道 FOPDT 参数 + 锚点表）
- out/figs/fig14_step_responses.png（A/B/C 三通道 × 湿/干 的阶跃响应与归一化曲线，叠加 exp_099 金标准）

## 7. 局限（预声明）

- PI 调度表若不可得，固定参数闭环的 G1 与真实闭环有偏差，G1 判定的解释降级为"动态形态近似"而非"逐点复现"
- 阶跃幅值 ±5% 阀位 / +2°C SP 为小信号假设；大阶跃非线性未测
- 工况点只取 2 个代表窗，不覆盖全工况空间
