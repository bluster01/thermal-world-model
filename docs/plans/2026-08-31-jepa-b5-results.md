# JEPA-B5 结果与审计（2026-08-31）

## 批次信息
- 矩阵：`configs/final_wm/jepa_b5_series_v1.json`（SHA `28dcb4b6`），协议 `jepa-b5-series-v1`
- 臂：c0（重训基线，与 v1 同配置同记录）+ b5（动作盲慢态：slow 更新去掉 physical-state 输入）
- 训练：seed0 / 120ep / validation-only 探索批；`paper_verdict_upgraded=false`
- 执行 commit：`0170d9f`（预注册链）→ 本批结果 commit：见 git log

## 终审数值（方向门原轨迹口径）

| 指标 | c0 | b5 | b2（对照，读状态版） | 门限 | 判定 |
|---|---|---|---|---|---|
| H18 终端 MAE | 0.6396 | 0.6114（−4.42%）| 0.6056（−5.31%）| ≤ c0×0.95 | ✗ 差 0.58pct |
| 负荷极差变化 | — | +10.9% | +0.7% | ≤ +10% | ✗ |
| 方向门 valve1 | −0.146（过）| −0.081，frac 1.0 | ✓ | mean<0 | ✓ |
| 方向门 valve2 | −0.105，frac 高 | **−0.010，frac 0.446** | +0.010，frac 0.294 | mean<0、CI上界<0 且 frac≥0.60 | ✗ |

**终审：`REJECT_EXPLORATORY_SEED0`（三门全挂，不升级论文）**

## 机制结论（B2+B5 组合证据链，供论文 §6）

1. **动作盲化后的均值不再反号**：b2（慢态读 physical state，state 是动作的函数）
   valve2 均值 +0.010（反号）；b5（不读 state）→ −0.010（方向回正）——
   该变化与慢态读取"动作效果"参与方向反转的假说一致，但单种子结构消融不能唯一识别主因。
2. **本次修复未消除效应强度稀释**：b5 阀2 均值 −0.010 vs c0 −0.105，弱约 10 倍；
   frac 0.446（近半窗分不清正负）。慢态守恒功率修正注入通道与阀 2
   喷水冷却效应共享同一守恒方程；结果**支持注入通道冲突假说**，尚不能证明与信息源无关。
3. **动作盲化后的关联变化**：精度收益从 b2 的 −5.3% 缩至 −4.4%（差 0.58pct 未过
   5% 门限），且负荷极差恶化 +10.9%；在本次单种子运行中与精度/稳健性退化同时出现。
4. **总判断**：B 系列共 7 个唯一臂（含 C0 与 B3-SHUFFLE；C0 执行两次），5 个
   可晋级候选均无 PROMOTE。单种子结果与表示增强—动作响应存在冲突的假说一致；
   不能据此声称机制已被因果识别或可跨机组外推。

## 后续动作
- B系列战役收线（B5 为最后一臂；B6 残差输入预计重复 B5 结局，不再执行）
- B2/B5 只作为 validation-only 探索证据："动作盲化后均值不再反号，但方向证书仍失败"；
  不写成已证明的因果破坏机制
- 数据/审计素材：`results/final_wm/jepa_b5_series_v1/{c0,b5}/`
