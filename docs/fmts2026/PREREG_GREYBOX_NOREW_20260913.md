# FMTS-GNR1：纯灰箱 no-rewet 补充实验

2026-09-13，作者在看到 v0.2 结果后提出。**探索性、结果后提出的单变量消融**，不是 v0.2 的追溯性预注册。
原协议、原 12 次结果及历史 verdict 不变；本文件在 GNR1 正式训练前冻结。

## 科学问题

原灰箱 H18 双阀响应为 +0.00448/+0.03305°C，与预期喷水降温方向不一致；融合模型则已消融再润湿。
不能以原比较建立“物理模型响应正确”或“学习修复方向”的结论。
本轮只问：**保持灰箱其余结构/拟合条件不变，关闭再润湿后，预测精度与识别出的模型阀门响应如何变化？**
即使全负，也不等于现场增益已校准；不得通过符号选择检查点、种子或窗口。

## 唯一新增训练臂

`greybox_steady_none_norew`，seeds 0/1/2。从 v0.2 `greybox_steady_none` 完整 spec 复制，
只改 arm 标识和 `closure_mode: none -> none_norew`；后者使 `aW1/aW2` 固定近零。
仍为稳态锚点、无神经功率闭合、无学习观察器、无额外潜变量。物理参数仍按原方法辨识。
不是在旧检查点上关开关就充当重新拟合后的基线；三个种子都从原规定初始化完整训练。

固定 24,000 更新上限（120×200）、batch32、Adam lr1e-3、五温度 NLL、每200更新验证、patience20。
按主汽温 equal-day cumulative H18 MAE 选最佳检查点，与原灰箱完全一致。
不重训黑箱、GRU 或 token，不调学习率/损失/预算/正则，不加入符号约束。

## 数据与评估

复用返回目录 `results/fmts_mainsteam_20260911/linux_full_v02`，校验 record/mapping/IAPWS 网格哈希及原源代码指纹。
原 `indices.npz` 逐字节复制，训练库20000、预测256、响应64不重新抽样；归一化重新计算后与原统计量核对。
保留 23 维历史合同，但灰箱仍只消费物理子集；不增加扩展变量的学习适配器。
H18、10秒采样；预测用记录的未来动作/基础边界，响应固定历史末值边界，单阀+0.05、上限1。
所有支持域违规保留。所有模型的主汽温 MAE/双阀响应仍只能解释为 oracle 条件下的模型诊断。
test 严格关闭。

## 分析规则（不是新的 PASS 搜索）

1. 新旧灰箱逐种子配对：累计 H1…H18 MAE、H18 MAE 差及1,000次日块区间。
2. 新旧灰箱双阀逐时响应、幅值、支持域违规、实际剂量、末端区间；全部三种子公开。
3. 新灰箱与已返回 GRU/黑箱配对，定位预测代价；GRU/黑箱结果不重跑、不另选检查点。
4. 负向末端均值/区间只作为描述性方向证据。保持“无现场真值”，不建立更易通过的新阈值。
5. 若方向仍异常或精度劣化，照实记录，不继续搜索；由作者决定下一步。

原灰箱应保留在附录/同一比较中。新版三模型主图是否将 no-rewet 灰箱作为匹配参考，由完整结果审计后决定，不能先假定它胜出。

## 执行/回传与论文状态

执行入口与审计：`experiments/fmts_greybox_norew_20260913/`。
准备顺序：保存注册 -> 验证只有再润湿差异 -> 合成 smoke/推理重放 -> 明确发布 -> Linux 正式三种子 -> 保存完整审计 -> 更新图表。
写稿阶段只保存 v0.2 证据初稿，GNR1 标为 pending，不写任何假设结果。正式训练仅由 Linux 执行。
