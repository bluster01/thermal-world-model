# 新稿结论—证据对应（内部审读）

这份稿件只用本次 `linux_full_v02` 建立主结果，不把旧五温度 MAE、H60 探针或单种子结果拼接成新结论。
统计来源均为 `audit_evidence.py` 独立读取 24 个 NPZ 后生成的 `source_data/*.csv`，不是从图上估数。

| 正文结论/位置 | 直接依据 | 限定与不允许的外推 |
|---|---|---|
| 摘要、§1：600 MW、12 年、预测前馈暴露精度/响应脱节 | 作者提供的工业背景，9/8 中文/英文旧稿 | 作者报告动机；不使用“百分之一”作为本轮实测真值 |
| §1：闭环相关性不能自动识别动作通道 | Forssell & Ljung 1999；controlled world models 已精读卡及原文 | 不换算条件激励到本厂 rho，不套用线性高斯界限 |
| §1：物理不足部分需要学习 | Fan2020 原文及旧稿笔记；方法实际闭合接口 | 不说 Fan 是完全白箱，也不宣称传统物理普遍失败 |
| §2：锚点+GRU 的慢状态有界修正 | `models.py:RichFusion.forward`，`observer.py:posterior`，`model.py:_correction_mask` | 只修金属温度和燃料滞后；不是重建真实隐藏状态的证明 |
| §2：物理动作通道及功率位置 | `transition.py`，`closure.py`，`observation.py` | MLP 不直接读动作但读动作相关状态；没有温度均值残差头 |
| §3：23 历史变量与过去扩展 | 预注册 v0.2、`spec.py`、`data.py`、Linux identity | 灰箱有效输入不同；9 扩展不代表 9 个新物理方程 |
| §3：未来 oracle 基础边界 | `run.py:evaluate(response=False)`、`models.py` | 黑箱用未来喷水总量，物理动作模式忽略；不等同在线可得输入 |
| 表1：H1/H6/H18 的 MAE 及种子标准差 | `prediction.csv`，`audit.json:aggregate` | cumulative 与 single-step 区分；先等日再等种子 |
| 表1：持续值 0.085/0.268/0.639 | 同一 NPZ 内 `persistence` 重算，所有 arm/seed 一致 | 已存证据的描述性参考，不是新增训练臂/选模判据 |
| §4：GRU 比黑箱 +0.0725°C、19.6% | `audit.json:derived` | 精确差值 0.07249962739118221；按 4 位小数报告，避免二次舍入 |
| §4：GRU 比灰箱 MAE 低 54.3% | 同上 | 完整方法比较；再润湿/初态/闭合/有效特征均不同 |
| §4：GRU 相对黑箱 2/3 日块区间高于零 | `paired_H18.csv`（GRU−blackbox 为反向） | seed1 区间包含零，不能写全三种子显著 |
| §4：黑箱双阀近零，GRU −0.147/−0.122°C | `response.csv`，`audit.json:aggregate` | 180 秒末端而非末十步平均；没有现场响应真值 |
| §4：GRU 6/6 末端日块区间严格负 | `audit.json:records` | 不代表每个窗口都负，更不是同时置信带或现场正确率 |
| §4：灰箱 +0.00448/+0.03305°C | 同一批原始响应数组 | 再润湿开启，不能仅归因于缺少学习 |
| §4：token MAE +47.7%，0/3 晋级 | 三种子配对 MAE、冻结 `TOKEN_ADVANCE_RULE` | 不根据响应视觉改判，不否定所有 Transformer |
| §3/附录：0/64 支持域违规 | 保存 masks 配对后重算，剂量全为 0.05 | 加宽后的边际区间非常弱，不保证状态-动作联合支持 |
| 附录：12/12 cap、最佳步与参数 | 各 arm 的 report 和 ledger | 不声称收敛、预算公平或所有稳定性均已验证 |
| 附录：本地 24 数组复算/哈希通过 | `audit.json` 与已有 `audit.py` 输出 | Linux 24 推理重放只有 commit 说明，本地不冒充再次推理 |

## 需要保留的缺口，及其不同目的

1. 锁定测试：回答独立时间段泛化，不校准现场动作增益。
2. 同再润湿灰箱对照：分离组件收益，不等同新的模型搜索。
3. 干预/自然实验响应校准：才可能建立真实方向/幅值误差。
4. 去 oracle：回答未来边界未知时的实际预测性能。

以上均未取得结果。作者在审读当前结果时已要求第 2 项：现已转入独立 FMTS-GNR1 注册、代码和 Linux 执行单，并按后续 push 指令发布，尚未收到执行回执。
其余项不构成本轮训练授权。论文是可审阅初稿，不是已获作者签字的投稿包。
