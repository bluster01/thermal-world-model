# STEP6 设计稿：级联传递函数提取 + 增益调度表 + 路线A/B设计

日期: 2026-08-17 | 状态: 预注册冻结 | 前置: FIX1-4 全链路收敛

## 1. 提取对象

- **执行机构 TF**: G_act(s) = 1/(τ_a·s+1), τ_a=883s (实测拟合 R²=0.978; 速率限制 0.14%/s 作为非线性项, 小信号用线性近似)
- **对象 TF** (阀位→温度): qnal 模型耦合阶跃 (v2+5% + W 按分状态 k_w 联动) → 差分响应 → FOPDT 拟合 G_p(s) = K·e^(−θs)/(τs+1)
- **工况点**: 测试段按 pm 选 6 点 (≈15.5/17.5/19.5/21.0/22.5/24.0 MPa 就近, 低波动优先), 全 5 通道 K/τ63, main 通道全 FOPDT
- **闭环 TF** (SP→main, 路线B用): G_cl = P·C/(1+P·C) 解析推导 + 数值验证

## 2. 预注册判定 (冻结)

- **T1**: main FOPDT 拟合 R² ≥ 0.85 于 ≥4/6 工况点 (不足者补双滞后拟合并报告)
- **T2**: 6/6 工况点 K_main < 0 (符号物理一致)
- **T3** (报告, 不设硬门槛): K_main 与 pm 的 Spearman 相关 + 调度曲线单调性
- **T4**: TF 级联闭环 (POU107 PI 按功率调度 + 执行机构 TF + FOPDT 内模) 6/6 工况点收敛: norm600∈[0.8,1.2] 且 tail_std≤0.05

裁决: T1∧T2∧T4 = PASS → TF 可用, 出路线A/B设计; FAIL → 报告归因。

## 3. 产物

out/step6_tf_summary.json (FOPDT 表 + 调度表 + 闭环验证), out/figs/fig20_tf_schedule.png, 路线A/B设计节
